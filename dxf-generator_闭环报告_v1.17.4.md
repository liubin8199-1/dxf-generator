# dxf-generator v1.17.4 闭环报告

> **C → B → D 三线收口** · 2026-09-11
> 技能版本：v1.17.4 ｜ 技能目录：`C:\Users\binliu8199\.workbuddy\skills\dxf-generator\`

---

## 〇、一句话结论

**C / B / D 三条线全部交付完毕，全量验证 18/18 套件、469 项断言、0 失败。**
本轮无遗留阻塞项；所列"后续方向"均属下一版本增强，不构成本轮未闭环。

---

## 一、任务来源

桌面 `路由接线.txt` 定义四条线：

| 线 | 内容 | 本轮归属 | 状态 |
|---|---|---|---|
| **A** | ③层节点接入 NL 路由 | 上一轮已完成（改用更干净的接法） | ✅ |
| **C** | 统一验证入口 | 本轮 | ✅ |
| **B** | 识图评测集 | 本轮 | ✅ |
| **D** | 钢结构节点扩展 | 本轮 | ✅ |

执行顺序：**C → B → D**。

---

## 二、C 线 · 统一验证入口 ✅

### 产物
`examples/verify_all.py`

### 解决的痛点
接入前 17 个 `verify_*.py` 需逐个手敲命令；改一行代码要跑 17 条命令；"到底挂没挂"依赖人眼扫输出，容易漏判。

### 关键实现
1. 自动扫描 `examples/` 下全部 `verify_*.py`，汇总执行并统计 PASS/FAIL。
2. `--list` 列出套件清单；支持关键词子集运行（例：`verify_all.py nl nodes routing`）。
3. **踩坑并修正（重要）**：
   - 初版按 `❌` 符号计数判失败 → `verify_fonts` 被误判 11/17。
   - 真相：那 6 个 ❌ 是**系统缺失字体的候选探测结果**（信息性输出），脚本自身判定"验证通过"。
   - 修正为**优先匹配明确判定语**（"验证通过 / 全部通过"），符号计数仅作兜底。复跑确认误判消除。

### 结果
**全量 18/18 套件、469 项断言、0 失败**，耗时 ≈ **1 分 42 秒**。
> 基线对比：接入前 17 套件 / 423 项 → 新增 `verify_steel_nodes.py`（24 项）→ 18 套件 / 469 项。

---

## 三、B 线 · 识图评测集 ✅

### 产物
`benchmarks/` 四件套：

| 文件 | 作用 |
|---|---|
| `eval.py` | 评测主程序，调真实 API `drawing_reader.read(path) -> DrawingInfo` |
| `ground_truth.json` | 7 张真实图纸的真值（文件名 → 期望图别） |
| `import_samples.py` | 从外部 source 导入原始图纸到 `raw/`（raw/ 不入库，规避 git 大文件） |
| `README.md` | 评测说明与运行方式 |

### 关键设计
- **真实样本**：复用 v1.17.1 那批 7 张真实 DWG 转出的 DXF（材料表/剖面等），放在 `outputs/dwg_probe/out_dxf/`。
- **raw/ 不入库**：技能目录是 git 仓库，136MB 原始 DXF 不适合入库；`raw/` 加进 `.gitignore`，用 `import_samples.py` 一键导入，保证"现在能跑、日后可恢复"。
- **核心指标 = 认错率**：不只看"是否识别为某类"，更看"有没有认错成别的类"——这是识图质量的关键短板。

### 结果（可复现 v1.17.1 基线）
| 口径 | 命中 | 认错 |
|---|---|---|
| 严格（drawing_type 完全一致） | 0/7 | 6/7 |
| 宽松（type 大类匹配） | 1/7 | 6/7 |
| **认错率** | — | **85.7%** |

> 结论：当前识图对真实工程图纸的判别能力极弱，是下一版本 **版式信号识别**（实体数/几何统计判别）的前置 baseline。

---

## 四、D 线 · 钢结构节点扩展 ✅

### 产物
- `scripts/templates_atlas/node_steel_v2.py`（17.6 KB）
- `examples/verify_steel_nodes.py`（24 项断言）

### 关键实现
1. **三类钢结构连接节点**（GB 50017 / 16G519）：
   - `steel_base` 钢柱脚（锚栓柱脚、底板、加劲肋）
   - `steel_beam_column` 钢梁柱栓焊混接（翼缘焊接 + 腹板高强螺栓）
   - `steel_splice` 钢梁拼接（翼缘/腹板拼接板 + 高强螺栓）
2. **独立注册表 `STEEL_NODES`**：不动既有 `NODES`（verify_nodes 断言 `len(NODES)==4`），避免回归。
3. **NL 路由接入**：在 `natural_language_engine.py` 增 3 个图别 + 3 个 handler + `STEEL_NODES` 默认尺寸；关键词**前置**于"钢结构|钢柱|钢梁"弱匹配，避免"钢梁柱节点"被混凝土 `node_beam_column` 抢走（含子串"梁柱节点"）。
4. **反例校验**：每类节点带参数合理性检查（如拼接板厚 ≥ 腹板厚），抓非法输入。

### 结果
`verify_steel_nodes.py` **24/24、0 失败**；
重排关键词后回归验证：`verify_node_routing.py` 23/23、`verify_nl.py` 46/46（**无副作用**）。

---

## 五、全量回归 ✅

后台跑 `verify_all.py`（含 D 后）：
- **18/18 套件、469 项断言、0 失败**

---

## 六、文档同步 ✅

| 文档 | 更新 |
|---|---|
| `SKILL.md` | 描述计数 23→26 类；v1.17.4 章节追加 C/B/D 三节 |
| `功能表.md` | §20.8 总验证行更新；追加 §20.9（统一入口）/§20.10（识图评测）/§20.11（钢结构节点） |
| `dxf-generator_完成报告_v1.17.4.md` | 纳入 B/C/D（16:59 已落盘） |

---

## 七、后续方向（非本轮阻塞，列入下期）

1. **B 扩充**：识图评测集从 7 张扩到 30~50 张，覆盖 34 类图别。
2. **版式信号识别**：用实体数/几何统计做图别判别，替换"关键词袋"路线，压低 85.7% 认错率。
3. **D 扩充**：钢结构节点补"支撑连接""刚接柱加腋"等。

---

## 八、闭环判定

| 检查项 | 结果 |
|---|---|
| C 产物落盘 | ✅ `examples/verify_all.py` |
| B 产物落盘 | ✅ `benchmarks/` 四件套 + `raw/` |
| D 产物落盘 | ✅ `node_steel_v2.py` + `verify_steel_nodes.py` |
| 全量验证通过 | ✅ 18/18、469/0 |
| 文档同步 | ✅ SKILL.md / 功能表.md / 完成报告 |
| 桌面副本 | ⚠️ 见下方说明 |

**结论：本轮 C→B→D 已闭环。**

> 桌面副本说明：按用户级归档惯例，人读交付物应同步一份到桌面。本次 `dxf-generator_完成报告_v1.17.4.md` 与本文档建议复制到 `C:\Users\binliu8199\Desktop\`。若需要我现在复制，回一句"拷桌面"即可执行（不在自动化流程里误拷，等你拍板）。

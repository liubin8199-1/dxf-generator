# dxf-generator 图纸工坊工作总结报告

> 编制日期：2026-09-13 ｜ 编制：小海（WorkBuddy）｜ 范围：v1.17.5 → v1.17.14（集中建设于 2026-09-12）
> 技能路径：`C:/Users/binliu8199/.workbuddy/skills/dxf-generator/` ｜ 评测分母：B 路线 external 真实出图 **n=26**

---

## 一、执行摘要

图纸工坊（dxf-generator）从"能出图"走到"出图 + 识图 + 造价贯通"全链路。本阶段最关键的成绩是**重建了识图评测体系**——把过去"认错率 85%→71% 看似进步"的**样本稀释假象**纠正为可对 external 真图量化的真实基线，并据此修掉了最大错误簇（火灾报警图误判 5 张清零）。

**关键数字（external n=26，唯一可信分母）：**

| 指标 | 数值 |
|---|---|
| 严格正确 | 4/26 (15.4%) |
| 宽松正确 | 2/26 (7.7%) |
| 正确拒识 | 3/26 (11.5%) |
| ★认错 | **17/26 (65.4%)** ← 核心指标 |
| 　其中 自信答错 / 空答 | 14 / 3 |
| 火灾报警图误判簇 | **0 张**（v1.17.13 前为 5 张） |
| 全量回归 `verify_all.py` | **21 套件 · 575 断言 · 0 失败** |

**一句话结论**：识图能力用外部真图量化到 认错 17/26、严格正确 4/26；最大错误簇已用窄①清零并立 before/after 门禁防回归；下一步卡在"外部样本扩到 30 张"。

---

## 二、技能概览

纯 Python / 离线 / `ezdxf` 驱动，把自然语言或参数化调用转成 **GB 国标 DXF 施工图**。

- **五大核心能力**：导入修改、参数化模板、批量处理、样式系统、自然语言分发（NL→26 类真实模板函数）。
- **专业模板库**：建筑（立面/剖面/节点大样/楼梯）、结构（钢筋符号/柱/板/基础/配筋）、机电（电气符号/电气图例/照明）、给排水、高级模板（总平/防火分区/空调/防排烟/雨水/火灾报警/智能化/横道/钢构）。
- **配套体系**：GB 标准图层/线型/字体（gbenor+gbcbig 杜绝中文 ??）、施工说明、规范引用（GB/JGJ 64 条×13 类）、图纸管理（图签/会签/门窗表/材料表）、六项自动审查、造价预算与采购清单。

---

## 三、识图评测体系（B 路线）从"假指标"修到"可量化"

### 3.1 样本污染事故（v1.17.6 更正）
把 13 张"确认真实"的桃花源系列导入评测集，总认错率 85.7%→71.4% 看似大幅进步。逐张取源后推翻：**这 13 张全是 `ezdxf` 脚本产物**（铁证 `$LASTSAVEDBY=ezdxf` + 4 张内嵌自述"AI 平面示意草图·非施工图"）。"改善"全部来自样本稀释——真实识别能力零改善。

### 3.2 样本取证铁律
- **`$LASTSAVEDBY` 比图层命名可靠**：真图是 `Administrator`/`妖怪`，自产是 `ezdxf`。
- **aspose 转换会抹元数据** → 来源闸门加**结构指纹兜底**：`$ACADVER ∉ {AC1024,AC1027,AC1032}` 且（实体≥1000 或 图层≥20）→ external。
- 闸门 `check_provenance.py --apply` 写回真值、`--gate` 不符即 `exit 1`，防自产样本漏进 external 分母。

### 3.3 度量口径三修（`eval.py`）
1. **占位符视同弃权**：`read()` 低置信返回字面量 `"未识别"`（非空）→ 旧口径 `refused` 桶结构性恒为 0（假数字）；现单列 `abstain`，`认错 = 自信答错 + 空答`。
2. **loose 排除 strict**：旧口径 5+6+0+… 桶不自洽（重复计严格）；现各桶互斥。
3. **分组报数 + 自洽校验**：`--external-only` 只评真实出图；`strict+loose+refused+abstain+errored+wrong == n` 内置断言。

### 3.4 真实基线（external n=26）
| 口径 | v1.17.6 (n=8) | v1.17.8 (n=26) |
|---|---|---|
| 严格正确 | 0 (0%) | **4 (15.4%)** |
| ★认错 | 7 (87.5%) | **19 (73.1%)** |
| 正确拒识 | 0 | **3 (11.5%)** |

⚠️ 87.5%→73.1% **不是能力提升**，是分母从 8 换成 26。最大错误簇 = **5 张被齐刷刷识成火灾报警图**（占 16 处自信答错 31%）。

### 3.5 防复发三件套（测试）
- `check_provenance`：来源闸门（来源取证）。
- `verify_prepare`（53 断言）：`dxf_prepare` 5 类"读不了"缺陷的负向对照+正向验收（合成 DXF 注入，秒级可进 CI）。
- `verify_abstain`（53 断言）：弃权能力纯函数断言，锁住默认关闭的阈值。

---

## 四、版本里程碑（v1.17.5 → v1.17.14）

| 版本 | 关键工作 | 验证 |
|---|---|---|
| v1.17.5 | 说明栏可读性回归修复（`verify_layout_notes` 70/70）；规范库 64×13→67×14；修 git 坏引用 | 19/19·469 断言·0 |
| v1.17.6 | **样本污染事故更正**；度量口径三修；`check_provenance` 闸门 | — |
| v1.17.7 | 别墅 188 DWG 勘察；修 aspose `BLOCKS` 漏写 SEQEND；结构指纹兜底；净可用 14 张 | 19/19·469·0 |
| v1.17.8 | 别墅样本入库（external 8→26）；修两类"读不了"（TABLES 失 ENDSEC / ENDBLK 空句柄）；**定位火灾报警图簇** | 20·522·0 |
| v1.17.9 | 识图**弃权能力**；阈值实证（置信度无效、**margin 才有判别力**）；反泄漏不传 filename | 21·575·0 |
| v1.17.10 | **四项交付能力贯通**：材料清单/施工说明/工程量/报价端到端；`bom_to_budget` 换算层（图纸驱动比方案级贵 58%） | 21·575·0 |
| v1.17.11 | 增强2 接单价库（草稿 bug 根除）；增强3 识图兜底；**修 v1.17.10 槽位回归**（`plan` 未登记 4.5，`%d` 截断） | 21·575·0 |
| v1.17.12 | 选项C：预算隐性假设显式化（人工=面积×650、机械=材料费×4%） | 21·575·0 |
| v1.17.13 | **P0-1 修法①**：火灾报警图簇 5→0（图例排除+图名级匹配）；认错 19→17、严格正确 3→4 | 21·575·0 |
| v1.17.14 | **P0-1 修法②③ before/after 实证驳回**（复活簇 0→5、认错 17→18）+ 回归门禁 `verify_fire_alarm_fix.py` | 21·575·0 |

---

## 五、P0-1 火灾报警图误判簇专项（本阶段重点）

### 5.1 问题
5 张非火灾报警图（01 材料表 / 02 通用剖面图 / 03 1F / 05 2F / 06 3F）被齐刷刷误判为火灾报警图，占 16 处自信答错 31%。

### 5.2 v1.17.13 窄①：实证定位 + 修复
- **定位污染源**：5 张触发文本全是 `火警警铃`（图例设备标注名），在图层 `L标注`，同位置 `(222.6,1065.2)` 模板复用；"火警"作为子串误中 "火警**警铃**" → 火灾报警图白得 +0.5，与真图名并列靠更早位置赢平局。
- **修复（通用语言规则，非针对文件名）**：
  - `_LEGEND_MARKERS=("图例",)` + `_clean_texts_for_typing()`：含"图例"的标注排除出打分 blob；
  - `_type_keywords_in_text()`：**图名级**匹配——命中须整串等于关键词或以图名后缀（图/平面图/剖面图/…）结尾，拒绝"火警"⊂"火警警铃"、"电气"⊂"机电图例"这类设备/材料名子串误中。

**before/after（eval.py --external-only，n=26）：**

| 指标 | before (v1.17.12) | after (v1.17.13) |
|---|---|---|
| 严格正确 | 3/26 | **4/26** |
| ★认错 | 19/26 | **17/26 (−7.7pp)** |
| 自信答错 | 16 | **14** |
| 火灾报警图误判 | 5 | **0（清零）** |

三项验收全过（认错降 / 非过拟合 / 无误伤），簇彻底消除。

### 5.3 v1.17.14 ②③：before/after 实证驳回
主人后续给三件套（用户版①图层名过滤 / ②火灾报警图关键词扩到 17 / ③火灾报警图图层信号），要求"落①②③、before/after、火灾报警 5→1-2 且严格正确不降即成功"。

- **实测 A（用户版①过度过滤）**：原样套用 `LEGEND_LAYER_KEYWORDS`（含 标注/说明/标题/图框/材料表/门窗表/轴号/dim）测 26 张 → **3250/18895 文本 (17%)** 被"图例层"过滤，峰值 29%（19 别墅_273结构）；真实图层 `L标注`/`A建筑标注`/`0-图框、标题、说明` 全中 → 房间名/标高/配筋被删。与"图例区不参与打分"初衷相悖 → 不替换窄①。
- **实测 B（②③ 上机）**：BEFORE=窄①；AFTER=窄①+②③。

| 指标 | BEFORE | AFTER | 判定 |
|---|---|---|---|
| ★认错 | 17/26 | **18/26** | ❌ 升 |
| 严格正确 | 4/26 | 4/26 | ➖ |
| 宽松正确 | 2/26 | 1/26 | ❌ 降 |
| 火灾报警图误判 | 0 | **5（复活簇）** | ❌ |

03/04/05/06/牧马 被重新喂成火灾报警图。单独回退③复测确认：**仅靠②就复活簇**（平面图含"探测器/总线/手报/声光"被②宽关键词子串命中）；③图层子串 `消防/报警/火灾` 同理更糟。根因：当前识图是**子串计数**，26 张里无一张真火灾报警图可增益 → 纯亏不赚。

- **判定**：❌ 误伤（簇复活）+ 认错升 → **驳回 ②③，保留窄①**。

### 5.4 沉淀铁律
1. **改识图规则必须用 `eval.py --external-only` 跑 before/after，且只对 external(n=26) 分母说话**（主人亲定）。
2. **中文子串匹配是识图头号污染**：`火警`⊂`火警警铃`、`电气`⊂`机电图例`、`探测器`⊂平面图设备标注、`消防/报警/火灾`⊂任意含这些字的图层；修法一律走"图名级后缀约束 + 图例区排除"，不要裸设备名/裸图层子串。
3. **用户给的修法也要拿数据拦**——即便主人亲自提的 ②③，before/after 显示复活误判簇也应驳回。

### 5.5 回归门禁
新增 `benchmarks/verify_fire_alarm_fix.py`：复用 `read()` 真实流水线 + eval 口径，验收 **火灾报警误判≤2 / 严格正确≥3 / 认错≤19**，达标退 0、否则退 1（可挂 CI）。当前态实跑 **✅ 达标**（0 / 4 / 17）。

---

## 六、四项交付能力（出图→识图→造价贯通）

| 能力 | 入口 | 数据源 | 说明 |
|---|---|---|---|
| 材料清单统计 | `bom.py · generate_bom()` | **图纸几何量**（真·量） | 按国标图层归并，算长/面积/个数 |
| 施工说明 | `construction_notes_v2.py` | 识图图别 + 规范库 | 24 类专业话术 + GB/JGJ 引用 |
| 工程量清单 | = 材料清单统计 | 图纸几何量 | 同源 |
| 清单报价 | `budget.py` + `bom_to_budget.py` | 图纸几何→定额量→估算 | **图纸驱动比方案级贵 58%** |

- **增强2 接单价库**（`price_library` / `price_library_nanning`）：草稿 `to_budget_prices()` 用 code 做 key 导致**静默空操作**（总价不变）→ 落地版改按材料名索引元组，四路对比证明真生效（南宁库 −0.4%~−4.2%）。
- **增强3 识图兜底**（`reader_fallback`）：草稿接口名（`DrawingUnderstanding`/`.confidence`）全错配 → 对照真实 `DrawingInfo`/`.type_confidence` 修正；margin≥0.2 且 conf≥0.7 判高置信，否则回退通用模板 + 顶部 `⚠` 警示。

⚠️ **诚实边界**：报价是方案级估算（面积×经验含量×经验单价），**非招标控制价/合同价/结算依据**；换算规则是经验假设非 16G101 定额；钢筋仍是体积×含钢量近似。

---

## 七、质量基线与回归防护

- 全量 `verify_all.py`：**21 套件 · 575 断言 · 0 失败**（自 v1.17.9 持平，每次改动都复跑）。
- **关键回归被抓出**（v1.17.11 副产品）：v1.17.10 插入 `[4.5] 报价` 却**未登记进 `plan`** → `steps`=9 而 `plan`=8、断言挂；`_plan_name` 回退 `"步骤%d" % 4.5` 把 4.5 **截断成 4** 显示撞名、`_is_enabled` 绕过开关。修复后该套件 57/57，全量零失败。
- 教训：**"改动很小"不等于"没有回归"**——回归结论只能以真实跑过的输出为准；分页 append 式新增步骤必须同步检查所有注册表。

---

## 八、当前优先级与下一步

1. **P0 扩 external 到 ≥30 张**（现 26，差 4）——翻任何阈值/加规则的前置硬条件。
2. **新 P0：`排水系统图` 第二污染**（03/05/06 因图内「排水/给水」标注命中，非①范围，待查）。
3. P1 external≥30 后定 `ABSTAIN_MARGIN_THRESHOLD` 默认值（候选 0.2）。
4. P1 把"危险率（自信答错率）"升为 `eval.py` 一等指标（弃权不降认错总数，须单列才看得出价值）。

---

## 九、总体诚实边界

- **识图仍弱**：external 认错 17/26 (65.4%)，距"可靠"很远；当前阶段目标是"把可量化、防回归"做扎实，而非吹识别率。
- **样本少（n=26 < 30）**：任何阈值/关键词调整都是过拟合，先扩样本。
- **全部 AI 生成图纸须经注册工程师复核**；报价非定额，仅供方案量级判断。

---

_报告基于 `benchmarks/eval.py --external-only`、`功能表.md`（§二十~§二十七）、`dxf-generator_完成报告_v1.17.5~v1.17.14.md` 及 git 提交记录（`1490451`→`d137372`）整理。所有数字均来自真实跑过的评测输出。_

# dxf-generator 完成报告 v1.17.8

**日期**：2026-09-12　|　**主题**：别墅图纸集 A 方案入库 · 又修两类「读不了」· 补上 `dxf_prepare` 的验证空洞
**上一版**：v1.17.7（又修两类读不了 + 来源闸门结构指纹兜底 + 别墅素材勘察）

---

## 一、成果一句话

把《百套别墅建筑设计CAD图纸》里内容确属**单一图别**的样本入库，
external 真实样本 **8 → 26 张**，首次跑出**完整的、可复现的真实基线**：
**严格正确 3 (11.5%) · ★认错 19 (73.1%)**，并且**首次出现「正确拒识」**。

---

## 二、交付清单

| 产物 | 说明 |
|---|---|
| `examples/dxf_prepare.py` | 新增 `repair_missing_endsec()` / `drop_empty_handles()`；`_repair_table_records()` 边界修正；守恒断言扩容 + 两道结构自检 |
| `examples/verify_prepare.py` | **新增**（53 断言）—— 覆盖 `dxf_prepare` 全部 5 类缺陷，合成数据、不依赖外部图纸 |
| `examples/verify_all.py` | 注册新套件（`SUITE_NAMES` 加 `verify_prepare.py: DXF 预处理`） |
| `benchmarks/eval.py` | 修 OOV 样本 `type: null` 导致 `TypeError` 崩掉整批评测的 bug |
| `benchmarks/ground_truth.json` | v1.3 → **v1.4**，39 条样本（external 26 / self_generated 13 / unknown 0），新增 `preprocess_note` / `baseline_note` |
| `benchmarks/raw/` | 新增 18 个 DXF（目录 gitignored，只提交真值表） |
| `SKILL.md` / `功能表.md` | 版本 → v1.17.8，补 v1.17.8 章节 / §21 |
| `outputs/别墅样本_入库与真实基线报告.md` | 人读交付物（含逐张明细 + 复现命令），已复制到桌面 |

---

## 三、修掉的两个 bug（含一个"我们自己造的"）

### 第 ④ 类：`TABLES` 段失去 `ENDSEC` —— ⚠️ 由 `dxf_prepare` 自己造成

| 项 | 内容 |
|---|---|
| 现象 | `18 别墅_别墅结构1.09.dxf` → `DXFStructureError: missing ENDSEC tag.` |
| 根因 | `_repair_table_records()` 只按"下一个 `NAMED_TABLES` 记录头"切记录边界 → **排在表末尾的无名记录把 `0 ENDTAB`/`0 ENDSEC` 一起吞进自己的记录体**，丢弃它时段尾标记陪葬 |
| 佐证 | 原文件另报 `DXFTypeError: name has to be a string, got NoneType`（= 最后一条表记录缺 `2` 名字组） |
| 修法 | ① **治本**：边界改为在 `ENDTAB`/`ENDSEC`/`TABLE` 处也切断；② **兜底**：`repair_missing_endsec()` |
| 结果 | `补ENDSEC=0`（源头已保住），strict ✅ |

### 第 ⑤ 类：`ENDBLK` 空句柄

| 项 | 内容 |
|---|---|
| 现象 | `24 别墅_E门窗.dxf` → `ValueError: Invalid handle .`；recover 只能刷屏 `skipped invalid handle "" in entity "ENDBLK"` |
| 根因 | `ENDBLK` 的 `5`（句柄）值为**空字符串**。空句柄在 DXF 里**任何情况下都非法** |
| 实测 | `E门窗` **12 处**、`E立面` **19 处** → 两张都从 recover 升到 **strict** |
| 修法 | `drop_empty_handles()` 删掉该 `5` 组，ezdxf 自动补新句柄 |

**效果**：别墅 24 张 **严格可读 24 / 需 recover 0 / 失败 0**（v1.17.7 时 21/3/0）。

---

## 四、最重要的工程修正：补上 `dxf_prepare` 的验证空洞

> `dxf_prepare` 从 v1.17.6 起累积了 5 类缺陷，却**没有任何套件覆盖它** ——
> 这正是第 ④ 类（自己吞 ENDSEC）能潜伏到这批别墅图才被撞出来的**根本原因**。

新套件 `examples/verify_prepare.py` 用**合成 DXF**（ezdxf 生成基线 + 文本层注入缺陷），
**不依赖任何外部图纸数据集**。每类必须过四关：

1. 注入后**确实读不了**（负向对照，证明缺陷注入有效）
2. `prepare()` 不抛异常且**组数守恒**
3. `prepare()` 后**严格模式能读**（正向验收）
4. 关键结构仍在（如 TABLES 段仍有 ENDSEC）

**53 断言全绿。** 两个注入陷阱（都已写进脚本注释）：
- 第 ② 类的基线里，**INSERT 后面必须再跟一个实体**。若后面直接是 `0 ENDSEC`，
  ezdxf 的链接子实体循环自然结束、**不报错**，缺陷复现不出来。
- 第 ④ 类必须**删掉 `2` 组**而不是置空串。空串仍是 `str`，ezdxf 不报错；
  只有组缺失才让 `name = None`，才会报 `DXFTypeError`。

---

## 五、真实基线（`eval.py --external-only`，external n = 26）

| 口径 | v1.17.6 (n=8) | **v1.17.8 (n=26)** |
|---|---|---|
| 严格正确 | 0 (0%) | **3 (11.5%)** |
| 宽松正确 | 1 | 1 (3.8%) |
| 正确拒识 | 0（桶**结构性恒为 0**） | **3 (11.5%)** ← 首次出现 |
| **★ 认错** | **7 (87.5%)** | **19 (73.1%)** |
| 　自信答错 | 7 | 16 (61.5%) |
| 　空答/弃权 | 0 | 3 (11.5%) |

⚠️ **87.5% → 73.1% 不是能力提升**，是分母从 8 张换成 26 张（含 14 张新别墅图 + 4 张 OOV）。
**明确禁止当作改进证据使用。**

### 两个可直接读出的信号

1. **首次出现「正确拒识」（3 张）**：3 张 3D/效果图**真的弃权了**。
   历史上 `refused` 桶**结构性恒为 0**（占位符 `"未识别"` 是非空字符串，永远进不了弃权桶）。
   → 待办「给 `read()` 加弃权能力」**已被证明可行**。
2. **最大错误簇：5 张被齐刷刷识成 `火灾报警图`**
   （`01 材料表`/`02 通用剖面图`/`03 1F`/`05 2F`/`06 3F`），占 16 处自信答错的 **31%**。
   已知是**模板图例污染** → 压认错率的**第一顺位靶子**，优先于调关键词。

---

## 六、入库构成

- **14 张单图别**：平面图 3 / 立面图 3 / 节点大样图 4 / 结构配筋图 3 / 结构图 1
- **4 张 OOV**：透视图 / 鸟瞰图 / 三维模型 / `E门窗`（词表无「门窗图」类）
- **剔除 5 张**（内容实为多图别套图）→ B 方案（真值 = 图别集合）的现成第一批数据
- ⚠️ 入库**保留原文件名**（如 `错层别墅建筑结构施工图` 的内容其实是**平面图**）——
  真实世界的固有现象，故意保留当真实场景

---

## 七、验证与提交

- `python examples/verify_all.py` → **20 套件 · 522 断言 · 0 失败**（约 1 分 21 秒）
- `python benchmarks/check_provenance.py --gate` → **exit 0**（external=26 / self_generated=13 / unknown=0）
- git：提交并推送 `origin/main`（提交后按惯例核验 `show-ref --heads` + `refs/heads/main` 非全零）

---

## 八、遗留（下一版）

| 优先级 | 事项 |
|---|---|
| **P0** | 查 **`火灾报警图` 误判簇**（占自信答错 31%），疑为模板图例污染 |
| **P0** | 给 `read()` **加弃权能力**（低置信返回空值而非占位符）—— 可行性已被 3 张正确拒识证明 |
| **P0** | 真实样本 **26 → 30 张**（<30 张调阈值必过拟合） |
| P1 | 统一 `raw/` 的预处理状态（基线 13 张未过 `dxf_prepare`、别墅 18 张已过）—— 会改历史基线，须单独评估 |
| P2 | B 方案：188 张全批 + 框架支持"真值 = 图别集合" |

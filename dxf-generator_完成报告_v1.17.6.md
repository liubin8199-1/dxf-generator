# dxf-generator 完成报告 · v1.17.6

**主题：B 路线认图评测集 —— 样本污染事故更正 + 度量口径三修 + 来源取证闸门**
**日期：2026-09-12**
**结论一句话：真实识别能力【没有任何改善】（严格正确 0/8、认错 87.5%）；上一轮看到的"改善到 71.4%"是样本污染造成的假象，现已定性、更正、并加了防复发闸门。**

---

## 一、本次做了什么（A 路线执行记录）

1. 从本机搜出的图纸里，按当时认为"确认真实"的标准，向 `benchmarks/` 扩样本：**7 张 → 21 张**
   （新增 13 张桃花源60# 系列 + 1 张真实 DWG `荣和大地店_平面布置图20200307.dwg` 经 aspose CONVERT 转 DXF）。
2. 跑评测，得到总认错率 **85.7% → 71.4%**，严格正确 0% → 23.8% —— 表面上是明显进步。
3. **但对新增样本逐张取源时发现：那 13 张全是 `ezdxf` 脚本产物，不是外部真实图纸。**

---

## 二、事故定性：13 张"真实图纸"实为自产

### 证据（`$LASTSAVEDBY` 是决定性信号）

| 样本组 | `$LASTSAVEDBY` | ACADVER | 图层数 | 实体数 | 判定 |
|---|---|---|---|---|---|
| 01~06 + 牧马中央空调（7 张） | `Administrator` / `妖怪` / `w` | AC1018/1021 | 120~469 | 10,027~29,361 | ✅ external |
| 荣和大地店（1 张） | （aspose 转换丢弃） | AC1018 | 2 | 3,050 | ✅ external（人工判定） |
| **桃花源60# 全系列（12 张）** | **`ezdxf`** | AC1024 | 8~14 | 198~375 | ❌ self_generated |
| **剖面图_1-1（1 张）** | **`ezdxf`** | AC1024 | 17（`SEC_*`） | 56 | ❌ self_generated |

**额外铁证**：4 张 `_平面示意_` 文件**内嵌自己的自述文字**：

```
· 外轮廓按面积 135㎡ 反算：13.5m × 10.0m
· 【AI 平面示意草图 · 非施工图】须现场测绘复核
· PDF 未提供本层房间划分，待现场测绘补录
```

### 为什么当初会误判为"真实"

当时的来源指纹只检验「图层名是否含 `G_` 前缀」（那是本技能自己的输出约定）。
而`AXIS / WALL / AREA / NOTE / TXT / SEC_*` 这些层名**同样出自脚本**，只是不叫 `G_*`，
于是全部被指纹放过了。

> **教训：`$LASTSAVEDBY` 比图层命名可靠得多。** ezdxf 写盘必然留下 `ezdxf`，
> 真实出图留下的是操作用户名。**另有一个量级信号：实体数差两个数量级**（自产 56~375 vs 外部 10,027~29,361）。

---

## 三、校正后的真实基线

| 分组 | 张数 | 严格正确 | 宽松正确 | 认错 |
|---|---|---|---|---|
| **external 真实出图** | **8** | **0 (0%)** | 1 (12.5%) | **7 (87.5%)**（自信答错 6 + 空答 1） |
| self_generated 自产 | 13 | 5 (38.5%) | 0 | 8 (61.5%) |
| 混合总计（不可作能力证据） | 21 | 5 (23.8%) | 1 (4.8%) | 15 (71.4%) |

**对照 v1.17.1（7 张真实）：严格 0/7、认错 6/7 (86%)。**
→ **严格正确仍是 0，认错率 86% → 87.5%，实质没有任何改善。**

自产组严格正确的 5 张全部来自「有干净图名文字或有专用图层前缀」的样本
（`室内立面图一/二`、`建筑立面图`、`地下一层平面示意`、`剖面图_1-1`）——系统认自己画的图自然更准。
**这 5 张恰恰证明了过拟合，不能用来宣称识别能力提升。**

---

## 四、度量口径三修（`benchmarks/eval.py`）

| # | 问题 | 处置 |
|---|---|---|
| **#1** | 「识别成功 100%」是假指标（只数"有没有输出"，不数"对不对"）——实测可同时成立「识别成功 100%」与「严格正确 0%」 | 一律报**认错率**（v1.17.1 已立，本轮沿用） |
| **#2** | `drawing_reader.read()` 低置信时返回**字面量占位符 `"未识别"`**（非空字符串）→ `inferred is None` 永不成立 → `refused` 桶**结构性恒为 0**（"正确拒识 0%"是假数字）；且"如实说不知道"与"自信答错"被**记为同一种错** | 占位符视同**弃权**（`answered=False`），单列 `abstain`；`认错 = 自信答错 + 空答`。`荣和大地店` 由此从 ❌ 改判为「…」（如实说不知道，严重度低于自信答错） |
| **#3** | `loose` 把 `strict` 重复计入 → 各桶之和 > n（5+6+0+1+0+14 = **26 ≠ 21**），无法构成对样本的划分 | `loose` **排除** `strict`；并内置**自洽校验**：`strict+loose+refused+abstain+errored+wrong == n`，不成立即显式报警 |

### 新增分组报数（防稀释的结构性闸门）

`eval.py` 现在**强制按来源分组**输出，并附警示：

```
【分组】外部真实 · 唯一能衡量真实识别能力的分母 —— 8 张
    严格正确 0/8 (0.0%) | 宽松 1 | ★认错 7/8 (87.5%)  [自信答错 6 + 空答 1]
【分组】AI/脚本自产 · 同源，不可作为能力证据 —— 13 张
    严格正确 5/13 (38.5%) | 宽松 0 | ★认错 8/13 (61.5%)  [自信答错 8 + 空答 0]
⚠️ 自产样本 13 张已计入总分 → 总认错率【被稀释】；报数请以【外部真实】分组为准。
```

新增 `--external-only` 开关：只评真实出图样本。**这是唯一能衡量真实识别能力的分母。**

---

## 五、新增防复发闸门 `benchmarks/check_provenance.py`

| 能力 | 说明 |
|---|---|
| 自动判定 | 优先级：内嵌自述（铁证）> `$LASTSAVEDBY=ezdxf` > 真人名 > 空则 `unknown` |
| 人工兜底 | `MANUAL` 表处理自动规则判不了的（aspose 转换会丢 `$LASTSAVEDBY` 与图层表，如 `荣和大地店`） |
| `--apply` | 把取证结论写回 `ground_truth.json`（新增 `provenance` / `provenance_evidence` 字段） |
| `--gate` | 与已记录结论比对；出现 `unknown` 或结论不符 → `exit 1`（可接 CI） |

**实测：21/21 全部定性一致，闸门通过。**

### 扩样本 SOP（已写进 SKILL.md / README.md）

```
import_samples.py  →  check_provenance.py --apply  →  --gate  →  才允许 eval.py
```
**未经取证的样本不得进入评测集。**

---

## 六、交付物清单

| 文件 | 变更 |
|---|---|
| `benchmarks/eval.py` | 五口径 + 分组报数 + 自洽校验 + `--external-only` |
| `benchmarks/check_provenance.py` | **新增**（来源取证闸门） |
| `benchmarks/ground_truth.json` | v1.1→v1.2：21 张全部补 `provenance` + 证据；更正 description |
| `benchmarks/README.md` | 重写：污染事故、校正基线、来源判定表、三修说明、扩样本 SOP |
| `SKILL.md` | 版本 1.17.4→**1.17.6**；description 与 B 章节更新；新增「v1.17.6」整节 |
| `功能表.md` | 版本→v1.17.6；20.10 更新；待办 #2 状态更正 + 新增 #2b「给 read() 加弃权能力」 |
| 临时文件 | 清理 10 个探针/脚本 + `examples/out_dwgconv/` |

---

## 七、下一步（优先级已重排）

| 优先级 | 事项 | 为什么 |
|---|---|---|
| **P0** | **扩真实样本到 30 张**（硬条件 `$LASTSAVEDBY != ezdxf`） | 8 张分母太小，任何阈值调整都还是过拟合 |
| **P0** | **给 `read()` 加弃权能力**（低置信返回空值，而非占位符 `"未识别"`） | **优先级高于调关键词**：一个从不承认"不知道"的系统，认错率不可能压到 0。这是唯一的路径 |
| P1 | 引入几何/版式特征（实体量级、HATCH 占比、闭合墙线数）替代纯关键词投票 | 外部图纸 10,027~29,361 实体 vs 自产 56~375，量级本身就是强判别信号 |

---

## 八、诚实边界

- 真实样本仅 **8 张**，其中 7 张来自**同一家设计院模板** → **结论不可外推到所有图纸**。
- 自产 13 张**只能用于验证"改坏没有"（回归），不能证明"识别变准"**。
- **识别率仍不可对外承诺**（当前严格正确 0%）。已承诺的只有：评测方法、口径纪律、来源闸门。

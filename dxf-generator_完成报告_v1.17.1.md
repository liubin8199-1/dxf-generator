# dxf-generator 完成报告 v1.17.1

**日期**：2026-09-11
**版本**：v1.17.0 → **v1.17.1**
**主题**：① 图纸空间布局选取规则正式化（评审①③落地）+ 续页命名待办（评审②）
　　　　 ② DWG 读取通用化 + **外部真实图纸识别率实测（②探针收官）**
**验证**：294 项全绿（零回归）

---

## 一、本次回应你的 4 条评审意见

| # | 你的意见 | 落地情况 |
|---|---|---|
| ① | `_pick_paper_layout` 选布局策略写进 SKILL.md 正式文档 | ✅ SKILL.md「一、布局选取规则」5 行优先级表 + 功能表 19.1 |
| ② | 续页命名 `GB_A3_说明2` 硬编码中文后缀 → 抽成可配置模板 | ✅ **记入待办**（SKILL.md 四.1 + 功能表 19.6#1），本次未改代码 |
| ③ | status 提升到 2 的注释显式引用 ezdxf `frontend.py::_draw_viewports()` | ✅ 注释已重写，含源码 4 行原文 + 行号级出处 |
| ④ | 剩余待办 ①②的优先级思考 | ✅ 见本文第四节 |

### 评①：布局选取规则（已正式化）

渲染 `space='paper'` 时的**固定优先级**（写死，不随调用变化）：

| 顺序 | 条件 | 结果 |
|---|---|---|
| 1 | 显式给 `layout='名字'` 且存在 | 用它 |
| 2 | 显式给名字但找不到 | `None` → 回退模型空间渲染 |
| 3 | 未给名字，存在**带 VIEWPORT** 的布局 | 取第一个带视口的 |
| 4 | 未给名字，无任何带视口布局 | 取**最后一个非 Model 布局** |
| 5 | 无任何非 Model 布局 | `None` |

两个要点：`doc.layouts` 第一个永远是 `'Model'`（**不是**图纸空间）；
判据是"**有没有 VIEWPORT 实体**"，不是布局名 —— 分页说明续页就是多个图纸空间，
靠名字猜会挑错。

### 评③：status 注释重写（已核对源码，不是凭记忆写）

出处：`ezdxf/addons/drawing/frontend.py :: _draw_viewports()`（ezdxf 1.4.4）

```python
viewports.sort(key=lambda e: e.dxf.status)
viewports = [vp for vp in viewports if vp.dxf.status > 0]
if viewports[0].dxf.get("status", 1) == 1:
    viewports.pop(0)          # ← 这一行丢掉了 status==1 的视口
```

其上方原注释写明：`status==1` = *the active viewport*，它
*determines how the paperspace layout is presented as a whole*，
所以 ezdxf 认为不必再单独绘制。**但 `1` 恰恰是磁盘上 AutoCAD 标准文件的正常值。**
注释里已把这四行源码与"只改内存副本、不写盘"的边界一并写清。

---

## 二、② 识图通用化：探针收官（这是本次的主交付）

### 2.1 样本

素材库 `2026农村自建房别墅设计素材合集` 取 **7 个真实 DWG**（同一套室内设计图）：
材料表 / 通用剖面图 / 1F / -1F / 2F / 3F / 中央空调新风地暖施工图。
**人工逐张标注了图别真值**，才能算出真确的识别率。

### 2.2 能做的（已打通并落地）

| 环节 | 结果 |
|---|---|
| DWG→DXF | ✅ 平均 **8.6 秒/文件**，体积膨胀 **7.8 倍**（均值 21.8MB）<br>→ 182 个文件约 **26 分钟 / 3.9GB**（可跑） |
| DXF→可读 | ✅ **7/7 全部可读**（修复前 2/7 崩或残） |
| 图层/文字 | ✅ 取到真实图层 120~469 个 + 真 TEXT 实体（带坐标） |

**关键转折**：之前判断"DXF 路径死了"（4MB DWG → 234MB DXF）是**误判 —— 模式选错了**。

| 模式 | 体积 | 文字 | 结论 |
|---|---|---|---|
| `RENDER`（默认） | 0.26MB → **12.7MB（49 倍）** | 炸成 POLYLINE，文字全丢 | ❌ 不可用 |
| **`CONVERT`** | 0.26MB → **1.59MB（6 倍）** | 保住真 TEXT/MTEXT | ✅ **必须用这个** |

### 2.3 不能做的（诚实结论）

用技能现成的 `infer_drawing_type` 打这 7 张真实图纸：

| 口径 | 结果 |
|---|---|
| 严格正确 | **0 / 7 = 0%** |
| 宽松正确（同族近似） | **1 / 7 = 14%** |
| **认错** | **6 / 7 = 86%** ← 识图里最该压到 0 的指标 |

典型错法：材料表 → 火灾报警图；各层平面 → 火灾报警图；暖通图 → 节点大样图。

> ### ⚠️ 度量教训（比数字本身更重要）
> 探针第一次跑出来是"**识别成功 7/7 = 100%**"。
> 这个指标是**假的** —— 它只统计"有没有输出"，不统计"输出对不对"。
> 加人工真值后，真实严格正确率是 **0%**。
> **结论：识图评测必须报"认错率"，不能只报"有没有识别"。**
> 这条已写进 SKILL.md，防止以后再被这个假指标骗。

### 2.4 三条根因（均用数据确认，不是猜的）

1. **规则只认自家图层命名。**
   `TYPE_BY_LAYERS` 匹配 `G_WALL`/`G_DOOR` —— 那是**本技能自己的输出约定**。
   真实图纸用 `A建墙`/`A门`/`IRC灯具`/`IFC地面分格` → 图层信号**零命中**。

2. **模板图例污染，且存在结构性矛盾。**
   这批图纸同出一家设计院模板，**189 行图例文字出现在全部 7 个文件里**
   （火警/排烟/空调风/总平面/大样…），文字信号被图例主导。
   更麻烦的是：**图纸自己的图名"首层平面图"本身就是模板行**（7/7 都有），
   所以"批量去模板"会把正确答案一起删掉。
   实测去模板只能把准确率从 14% 拉到 29%，且仍有短通用词误命中残留图例。

3. **图层是"文档级"而非"图纸级"。**
   AutoCAD 图层在整个 DWG 里定义一次。实测 6 张图各带 120~469 个图层，
   **且共享同一套**（`IRC天花造型`/`A建墙`/`A门`… 每张都一样）。
   → 图层名只能判"这是室内设计图纸集"，**判不出是平面还是剖面**。

### 2.5 可判别信号在哪（下一步方向）

在**版式/几何**，不在关键词：

- `01 材料表` 模型空间只有 **262** 个实体（主体在 Layout1 的 734 个）；
  `03 1F` 模型空间有 **29,361** 个实体 → **实体数量级本身就是强信号**。
- 各图别版式特征：材料表 = 表格线框；剖面 = 水平构造层 + 引出线；
  平面 = 闭合墙线 + 轴网。所需几何统计（`LWPOLYLINE`/`HATCH`）技能已具备。

**但**：**7 张样本调出来的阈值一定是过拟合。**
必须先抽 **30~50 张**建评测集（统一放 `benchmarks/`，标真值），再动规则。

### 2.6 边界（对外必须写清）

- ✅ 可承诺：**读得进**外部 DWG/DXF（转换 + 容错加载 + 中文转义解码）。
- ❌ 不可承诺：**认得出**外部图纸的图别（当前 0~14%，且 **86% 会自信认错**）。
- 自家产物（`dxfkit` 生成、`G_*` 图层体系）识别率不受影响。

---

## 三、代码改动清单

| 文件 | 改动 | 性质 |
|---|---|---|
| `scripts/drawing_reader.py` | 新增 `_decode_uesc()` 解码 `\U+XXXX`；新增 `_load_dxf_tolerant()`（严格失败退 recover）；无名图层跳过；`import re` | 健壮性 |
| `scripts/interfaces.py` | status 注释重写（引用 ezdxf 源码出处） | 注释 |
| `examples/dxf_prepare.py` | **新增**：DXF 预处理（剥 OBJECTS 段 + 删缺名表记录），**按 (code,value) 成对解析** | 新文件 |
| `examples/dwg2dxf_convert.py` | **新增**：DWG→DXF（`CadOutputMode.CONVERT`） | 新文件 |
| `SKILL.md` | version → 1.17.1；新增两章（布局规则+通用化 / ②实测结论） | 文档 |
| `功能表.md` | 标题 → v1.17.1；新增第十九章 | 文档 |

### 3.1 三个"不踩就会白干"的技术坑（已固化进文档）

1. **`CadOutputMode.CONVERT`**：默认 `RENDER` 会把文字炸成 POLYLINE、体积涨 49 倍。
2. **DXF 必须按 (code, value) 成对解析**：DXF 的"**值**"行本身可以是 `0`
   （实测 `281`/`0`、`90`/`0`、`1071`/`0`）。
   我第一版按"行内容等于 0"判记录边界，把记录切错位，
   全 7 个文件报 `Invalid group code "Continuous"` —— 改成成对解析后 7/7 通过。
3. **aspose 的 OBJECTS 段 ezdxf 读不了**（VISUALSTYLE 的 291 组码 / XRECORD 缺 y 坐标），
   整段剥掉即可；`05 2F` 另有 2 条缺名字的表记录需一并删掉。

### 3.2 验证

`verify_layout_notes` 69 + `verify_v114` 60 + `verify_pipeline_v115` 57 +
`verify_nl` 46 + `verify_v115` 37 + `verify_interfaces` 21 + `verify_pipeline` 4
= **294 项全绿**。

---

## 四、评④：剩余待办的优先级思考

### 待办清单

| # | 待办 | 投入 | 产出性质 |
|---|---|---|---|
| ① | **标准图册（16G101 平法节点库）** | 1~2 天 | 扩"**能画什么**" |
| ② | **识图通用化（完整版）** | 1.5~2 天（含建评测集） | 扩"**能读什么**" |
| ③ | 续页命名模板化 | 0.5 小时 | 工程整洁 |

### 我的倾向：**先做 ①，② 排其后**

理由三条：

**1. ① 是"确定性收益"，② 是"不确定性投入"。**
16G101 平法节点是有**明确国标依据**的确定性工作：规范怎么写的、节点怎么画的，
查得到、可核对、验收标准清晰。做完就是行业刚需能力。
而 ② 的识别率是统计问题，本样本已经证明 86% 会认错 ——
投入 1.5 天能不能做到 80% 准确率，**现在无法承诺**。

**2. ② 的前置条件还没满足，现在做必然过拟合。**
要在 7 张样本上把识别率调到好看很容易，但那只是背下了这 7 张。
正确顺序是：先建 **30~50 张评测集**（这是"体力活"，0.5 天且无技术风险），
拿到基线后再决定规则怎么改。
**把"建评测集"和"改规则"解耦**，是避免自欺的关键。

**3. 从使用频率看，① 的触达更广。**
出图是本技能的主线（技能定位就是"自然语言→国标施工图"），
平法节点库直接让主线产出更完整。
识图是支线（"读懂别人给的图"），且 §2.6 已明确边界 ——
**读得进**已经够用，**认得出**是锦上添花。

### 如果反过来先做 ②，建议这样切

不直接改识别规则，而是：
1. 先花 0.5 天建 `benchmarks/`（30~50 张 + 真值表 + 评测脚本，**可复跑**）；
2. 拿到基线数字后**再找你拍板**是否继续投 1~1.5 天做版式信号。

这样即使中途停手，也留下了可复用的评测集，不算白做。

### ③ 续页命名模板化

0.5 小时，纯工程整洁，不阻塞任何事。
建议**顺手做掉**（放在 ① 开工前的热身，或与任一任务合并提交）。

---

## 五、产物

| 文件 | 说明 |
|---|---|
| `SKILL.md` / `功能表.md` | v1.17.1 文档（含本轮全部结论） |
| `examples/dxf_prepare.py` | DXF 预处理层（幂等、纯标准库） |
| `examples/dwg2dxf_convert.py` | DWG→DXF 转换器（cadconv venv） |
| `scripts/drawing_reader.py` | 通用化读取（转义解码 + 容错加载） |
| `outputs/dwg_probe/` | 探针全套脚本与中间产物（7 组 DWG/DXF + 5 个探针脚本） |

# dxf-generator 完成报告 v1.17.9

**主题**：识图「弃权」能力落地 + 阈值实证
**日期**：2026-09-12
**上游**：v1.17.8（别墅样本入库，external n=26，真实基线已出）
**核心命题**：压认错率继续调关键词 = 小样本过拟合；唯一可行路径是**让识图学会"不知道就说不知道"**

---

## 一、结论先行

| 项 | 结果 |
|---|---|
| 弃权能力 | ✅ 已落地（`DrawingInfo` 新增 3 字段 + 3 个新 API） |
| 历史基线 | ✅ **逐桶逐字未变**（默认门槛 0.0，幂等性经实证） |
| 阈值实证 | ★ **置信度阈值是空操作**；**决策裕度 margin 才有判别力** |
| 若开 `margin≥0.2` | 自信答错 **16→3（-81.3%）**，严格正确 **3→3（零损失）** |
| 默认是否开启 | ❌ **不开启** —— n=26、严格正确仅 3 张，任何阈值都是过拟合 |
| 验证 | **21 套件 · 575 断言 · 0 失败**（新增 `verify_abstain.py` 53/53） |

---

## 二、用户提案 vs 代码现实：4 处不一致（已按现实修正）

用户给的改动草图有 4 处与真实代码不符，**直接照抄会跑不起来**：

| # | 草图写的 | 代码现实 | 处理 |
|---|---|---|---|
| 1 | 类名 `DrawingUnderstanding` | 真实类名 **`DrawingInfo`** | 用 `DrawingInfo` |
| 2 | `drawing_type: Optional[str] = None` | 真实默认 **`"未识别"`**（字符串占位符） | **保留占位符**（见下方理由） |
| 3 | 新增 `confidence` 字段 | **已存在**，名为 `type_confidence` | 复用，不新增 |
| 4 | 新增 `abstained` 字段 | ✅ 确实不存在 | **新增**（另有 `type_margin` / `abstain_reason`） |

### ★ 为什么弃权时**不写 `None`**（第 2 点的关键）

草图的 `u.drawing_type = None` 看起来更"干净"（能区分"未识别"与"没答案"），
但会破坏下游契约：

```python
# pipeline.py:495     self.result.drawing_type_cn = info.drawing_type or ""
# pipeline.py:522     info.drawing_type or "未识别"
# construction_notes_v2.py:19  ...match_by_drawing_type(info.drawing_type)
# drawing_reader.to_dict() / to_markdown()  直接把它吐出来
```

下游**全部按字符串处理**。改 `None` 会让这些调用点从"拿不到答案"退化成"崩掉"，
而收益（区分"未识别"与"没答案"）**已由 `abstained` 布尔字段完全提供**。

→ **决策：占位符保持原样，弃权语义另立字段。** 这是本版最重要的设计取舍。

---

## 三、★ 阈值实证：为什么不能照抄 `ABSTAIN_THRESHOLD = 0.3`

`ABSTAIN_THRESHOLD = 0.3` 是拍脑袋的数。先量了数据再定值 —— 结论是**这个数在这里是空操作**。

### 3.1 置信度只有 3 个离散取值，且对错完全重叠

```
conf 取值分布 : {0.0: 6 张, 0.5: 14 张, 0.7: 6 张}
严格正确的 conf: [0.5, 0.5, 0.7]                    ← 3 张
答错的     conf: [0.0, 0.0, 0.0, 0.5×10, 0.7×3]     ← 16 张
```

**完全重叠，没有任何阈值能切开。**

### 3.2 阈值扫描（`eval.py --external-only --sweep`，走真实代码路径）

| 阈值 (conf, margin) | 严格正确 | 宽松 | 拒识 | 空答 | 自信答错 | ★认错 | 危险率 |
|---|---|---|---|---|---|---|---|
| **(0.0, 0.0) 现状** | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.3, 0.0) ← **用户提案** | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.4, 0.0) | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.5, 0.0) | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.6, 0.0) | **1** ↓ | 0 | 4 | 16 | 5 | **21 (80.8%)** ↑ | 19.2% |
| **(0.0, 0.2)** ← 实测最优 | **3** | 0 | 4 | 16 | **3** | 19 (73.1%) | **11.5%** |
| (0.0, 0.3) | **2** ↓ | 0 | 5 | 19 | **0** | 19 (73.1%) | 0.0% |
| (0.7, 0.2) | 1 ↓ | 0 | 4 | 18 | 3 | **21 (80.8%)** ↑ | 11.5% |

**读法**：
- 用户提案的 **0.3 / 0.4 / 0.5 与"关闭"逐桶完全相同** ——
  因为 `conf < 0.3` ⟺ `conf == 0` ⟺ 本就无任何命中 ⟺ **早就返回占位符了**。
- 提到 **0.6 反而更差**：把对的也弃了（严格 3→1、未答对 19→21）。
- 换 **margin ≥ 0.2** 才见效：自信答错 **16→3**，严格正确 **3→3**。

### 3.3 为什么 margin 行得通

`margin = top1 - top2` 衡量的是"**赢得多干净**"，而不是"分多高"。

```
margin 取值分布: {0.0: 20 张, 0.2: 4 张, 0.5: 2 张}
26 张里 20 张 margin == 0（并列），其中 16 张（80%）是自信答错 ← 答错高发区
```

> **"赢得很勉强"比"分数低"更值得警惕。**

`margin ≥ 0.5` 的 2 张是全批**唯二"只有一个候选类别得分"**的图，2/2 全对。

### 3.4 ⚠️ 弃权**不会**降低 `★认错`

```
fail = 自信答错 + 空答 + 读取异常
```

弃权只是把「危险错误」**搬进**「空答」桶 —— **总数恒为 19**（73.1% 不变）。
要看清这一点，必须**单列「危险率（自信答错率）」并把它当第一指标**。
否则会误以为"弃权没用"。

---

## 四、最终决策：机制先落地，默认关闭

```
ABSTAIN_CONF_THRESHOLD   = 0.0   # 不启用
ABSTAIN_MARGIN_THRESHOLD = 0.0   # 不启用
```

**理由**：n=26、严格正确仅 3 张 —— **任何阈值都是过拟合**
（`eval.py` 自己就印这句警告）。默认 0.0 保证：

1. 历史基线可复现（v1.17.8 的 3/1/3/3/16/19 数字继续有效，**经实测验证**）；
2. 阈值可从数据出发再定，而不是把拍脑袋的数焊死进代码；
3. `verify_abstain.py` 里**写死断言锁住默认值**，防以后被无意改掉。

**翻转默认值的前置条件：先把 external 扩到 ≥30 张。**

---

## 五、代码改动清单

### `scripts/drawing_reader.py`
- `DrawingInfo` 新增：`type_margin` / `abstained` / `abstain_reason`
- 新增 `PLACEHOLDER_TYPE = "未识别"`（原先是散落的字面量）
- 新增常量 `ABSTAIN_CONF_THRESHOLD` / `ABSTAIN_MARGIN_THRESHOLD`（默认 0.0）
- 新增 `score_drawing_types()` —— 返回**全部候选**得分
  （原 `infer_drawing_type` 只看 top1，**看不到 margin**）
- 新增 `infer_drawing_type_ex()` —— 四元组 `(类型, 置信度, 裕度, 证据)`
  **旧 `infer_drawing_type` 三元组签名不变**，内部转调 `_ex`（向后兼容）
- 新增 `set_abstain_thresholds()` —— 运行时改门槛（评测扫阈值不必改源码）
- `_infer_type()` 加弃权判定；`to_dict()` / `to_markdown()` 暴露新字段
- ★ **`_infer_type()` 继续不传 filename**（反泄漏，见 §六）

### `benchmarks/eval.py`
- 优先读 `info.abstained`，**保留占位符兜底**（老 reader 无该字段时降级，两条路径等价）
- 新增 `--abstain-conf` / `--abstain-margin` / `--sweep`
- 修契约瑕疵：OOV 样本 `truth==""` 时 `truth and truth in inferred` 求值成 `""`
  → JSON 出现 `"loose": ""`（假值但是字符串）→ 六桶统一套 `bool()`
- 新增**度量教训 #5**：置信度不该当门槛，裕度才该

### `benchmarks/ground_truth.json`（v1.4 → **v1.5**）
新增 `abstain_note`（643 字）记录全部阈值实证结论 + 默认关闭理由。

### `examples/verify_abstain.py`（★ 新增，53 断言）
### `examples/verify_all.py` —— 注册 `'verify_abstain.py': '识图弃权能力'`
### `SKILL.md` / `功能表.md` —— 版本 1.17.9 + 新增章节

---

## 六、★ 反泄漏：用**行为**证明文件名没被当信号

**背景**：评测集 26 张里 **6 张文件名直接含真值关键词**
（如 `11 别墅_立面图.dxf`、`07 别墅_多层别墅平面图.dxf`）。
`infer_drawing_type` 的 `filename` 参数权重高达 **0.8**（最强信号），
但 `DrawingReader._infer_type()` **刻意不传它**。

若传了，严格正确会从 **3/26 虚增到 9/26** —— **那是把答案喂给模型，指标就废了**。

**测试做法（行为级，不读源码）**：
> 造一张名为 `平面图.dxf` 但图内**零平面图信号**（无 `G_WALL`/`G_DOOR`/`G_WINDOW`、无文字）的图
> → **必须弃权**。若泄漏，它会答"平面图"。

另加反向对照：同一图形内容换个不含关键词的文件名，结论必须**完全一致**。

> 为什么不用"读源码断言 `filename=` 没出现"？——那种断言在正常重构
> （改参数名/拆函数）时会产生**假阳性**，而行为断言只关心真实可观察结果。

---

## 七、验证

### 7.1 新增套件 `verify_abstain.py` —— 53/53

| 组 | 断言 | 覆盖 |
|---|---|---|
| [1] 默认值与占位符契约 | 7 | 3 新字段默认值、`PLACEHOLDER_TYPE`、**门槛默认 0.0 锁死** |
| [2] 接口兼容 | 6 | 三元组不破、四元组新增、分布函数自洽、filename 参数仍可用 |
| [3] margin 语义 | 5 | 单一命中/并列/部分领先/零命中 四种情形 |
| [4] `set_abstain_thresholds` | 4 | 单项覆盖、返回现值、归零=关闭 |
| [5] 端到端（门槛关闭） | 6 | **行为与改动前逐字一致** |
| [6] margin 门槛 | 6 | 并列图弃权、干净图不受影响、理由/证据/置信度保留 |
| [7] conf 门槛 | 3 | 低分图弃权 |
| [8] 反泄漏 | 3 | 行为级证明文件名未被使用 |
| [9] 序列化 | 7 | `to_dict` / `to_markdown` 新字段 |
| [10] 不变式 | 6 | `abstained` ⇔ 占位符（eval 两条判定路径等价的前提） |

### 7.2 基线幂等性（★ 最关键的一项）

改动后 `eval.py --external-only` **与 v1.17.8 基线逐桶逐字完全相同**：

```
严格正确 3/26 (11.5%) | 宽松 1 | 正确拒识 3 | 空答 3 | 自信答错 16 | ★认错 19/26 (73.1%)
桶自洽   : 26 == 26 ✅
```

→ 证明"默认关闭"不是声明，而是**实测事实**。

### 7.3 模拟保真度

`--sweep` 为了省时间用「先读一遍 + 纯模拟」（弃权是 `f(conf, margin)` 纯函数），
必须验证模拟可信 —— `--abstain-margin 0.2` **实跑**结果：

```
严格正确 3 | 宽松 0 | 正确拒识 4 | 空答 16 | 自信答错 3 | ★认错 19
```

与模拟输出 **（3, 0, 4, 16, 3, 19）逐桶一致** ✅

### 7.4 全量验证

**21 套件 · 575 项可计数断言 · 0 失败**

```
✅ 识图弃权能力 53/53（新增）   ✅ 布局+说明栏 70/70    ✅ 识图/算量/说明 60/60
✅ NL 流水线 57/57             ✅ 平法标注 53/53       ✅ DXF 预处理 53/53
✅ 自然语言解析 46/46           ✅ 3D 体量 37/37        ✅ ③层节点大样 29/29
✅ 钢筋计算 26/26               ✅ 钢结构节点 24/24     ✅ 节点 NL 路由 23/23
✅ 接口层 21/21                 ✅ 梁柱节点 12/12       ✅ 续页命名 7/7
✅ 端到端渲染 4/4               以及 高级模板+审图 / e5 / 字体配置 / 全量扩展 / 说明全套（按判定语通过）
```

---

## 八、下一步（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| **P0** | **扩 external 到 ≥30 张** | 8 → 26 已做；差 4 张。**不达 30 张，任何阈值都不能定值** |
| **P0** | 查 `火灾报警图` 误判簇 | 5 张（占 16 处自信答错的 **31%**），疑为**模板图例污染**。压认错率的**第一顺位靶子** |
| P1 | 定 `ABSTAIN_MARGIN_THRESHOLD` 默认值 | 样本 ≥30 后据数据翻转；候选 **0.2**（当前实测最优） |
| P1 | 把「危险率」提升为 `eval.py` 的一等指标 | 当前混在 `★认错` 里，弃权的价值看不出来 |
| P2 | 统一 `raw/` 预处理状态 | 基线 13 张仍带 OBJECTS 段，别墅 18 张已过 `prepare`（会改历史基线，须单独评估） |

---

## 九、诚实披露

1. **本版没有提升识别率。** `strict` 仍是 3/26，一个都没多。
   本版交付的是**度量与安全机制**，不是能力提升。
2. **`margin≥0.2` 的"-81%"不能对外承诺。** 它来自 26 张样本、3 张正例，
   置信区间极宽。文中所有阈值数字**只用于判断方向，不用于定值**。
3. **弃权不降低 `★认错`。** 若只盯 `★认错` 会得出"弃权没用"的错误结论；
   应看**危险率**（自信答错率）。
4. **`火灾报警图` 误判簇未修**（5 张 / 31%），是本版明确留给下一版的靶子。

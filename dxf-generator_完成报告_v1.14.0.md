# dxf-generator Skill 完成报告 v1.14.0

| 项 | 内容 |
|---|---|
| 技能名 | `dxf-generator` 图纸工坊 |
| 版本 | **v1.14.0**（v1.13.1 → v1.14.0） |
| 技术栈 | 纯 Python / 离线 / ezdxf 1.4.4 + matplotlib + openpyxl |
| 报告日期 | 2026-09-10 |
| 主题 | 补齐「识图 / 清单统计 / 施工说明 v2 / 电气暖通专业洞」四大板块 |
| 状态 | ✅ 全部落地，验证 60/60，零回归 |

---

## 一、本次做了什么

此前技能的能力是**单向的**：只会"出图"（自然语言/参数 → DXF）。
本次补上了**反方向**——把一张 DXF **读懂**、**算量**、**配说明**，形成完整闭环：

```
出图（原有 19 类 NL + 全专业模板）
   ↓
识图（新）    读 DXF → 图别 / 图层 / 文字 / 几何 / 体检
   ↓
算量（新）    读 DXF → 工程量清单（CSV/JSON/Excel/MD）
   ↓
说明（新）    图别 → 匹配专业施工条款 + 引用规范
```

同时补掉**电气 / 暖通两个专业洞**。

---

## 二、新增模块

### 2.1 识图引擎 `scripts/drawing_reader.py`

| 能力 | 说明 |
|---|---|
| 文件元信息 | DXF 版本 / 单位 / 全部布局 |
| 图层清单 | 按国标专业归类（G/S/P/E/图框/自定义） |
| 实体分类 | 线/圆/弧/多段线/文字/标注/块/填充 + 按空间分布 |
| 文字提取 | TEXT + MTEXT（自动剥离格式码） |
| 块引用统计 | 块名 → 次数 |
| 尺寸标注 | 全部 DIMENSION 测量值（范围/平均） |
| 几何量算 | 各图层线长、闭合面积、块数、模型空间 extents |
| **图别推断** | 三信号源加权：文件名 0.8 > 图内文字 0.7/0.5 > 图层特征 0.4 |
| 完整性体检 | 图框/标题栏/文字层/标注层/中文样式/量纲一致性 |

**修复的两个关键 bug（自测时暴露）**：
1. **图别误判**：柱配筋图被判成"电气图"。根因是**图层声明 ≠ 图层有实体**——国标图纸一次性声明 66 个图层，其中 E_*/P_* 实体数为 0 却参与了推断。修复：只用 `entity_count > 0` 的图层推断。
2. **量纲误标**：部分文件 `$INSUNITS` 标"米"但实际按毫米绘制。修复：用模型空间 extents 反推（A3 图框实测 420×297），矛盾时出警告。

### 2.2 清单统计 `scripts/bom.py`

- **28 类**国标清单归并规则（墙体/门窗/楼梯/钢筋/梁/柱/板/基础/给水管/排水管/消防管/强电/照明/电线/防雷接地/标注/图框…）
- 计量：线状累计长度(m)、闭合多段线与圆累计面积(m²)、块与文字累计个数
- 导出 **4 种格式**：CSV / JSON / **Excel（含分类汇总页）** / Markdown
- 与 `drawing_management.QuantityList` 的分工：那个是**手工填数**的表骨架；本模块是**从图纸自动算量**

### 2.3 施工说明 v2 `scripts/construction_notes_v2.py`

- **28 类图纸 / 143 条条款 / 43 部规范**
- 覆盖：建筑 / 结构 / 给排水 / 暖通 / 电气 / 消防 / 施工管理
- **识图联动**：`match_by_drawing_type(识图结果)` 直接映射到话术 key，实现"读完图自动配说明"

> 与老 `construction_notes.py` 分工：老的按 **GB 系列专业**生成，v2 按 **图纸类型**精确匹配。并存互补。

### 2.4 电气 / 暖通专业洞

| 模板 | 函数 | 出图规模 |
|---|---|---|
| 配电系统图（单线图） | `dist_power_system` | 113 实体 |
| 防雷接地图 | `lightning_grounding` | 46 实体 |
| 空调水系统图 | `chilled_water_system` | 80 实体 |
| 风管平面图 | `duct_plan` | 69 实体 |

统一注册表：`dxfkit.SPECIALTY_TEMPLATES`。

**重要适配说明**：外部参考代码用的是 `b.rectangle()` / `b.circle()` / `b.line(linewidth=)`——本库**没有这些接口**。实际接口为 `add_rectangle(x,y,w,h)` / `add_circle(cx,cy,r)` / `line()` 不支持线宽参数。全部已按真实 API 重写。

---

## 三、验证结果

### 3.1 新增能力验证 `examples/verify_v114.py`

| 组 | 内容 | 结果 |
|---|---|---|
| [1] 识图引擎 | 15 张全识别、Markdown/JSON 导出、extents 采集 | PASS |
| [2] 清单统计 | 10 张全出清单、4 格式落盘、图层归类 | PASS |
| [3] 施工说明 v2 | 28/28 类可生成、识图联动、143 条款 | PASS |
| [4] 电气专业洞 | 配电 113 / 防雷 46 实体，带图框 | PASS |
| [5] 暖通专业洞 | 空调水 80 / 风管 69 实体，带图框 | PASS |
| [6] 主链集成 | dxfkit 导出 20 个新符号 | PASS |
| | **合计** | **60 / 60** |

### 3.2 全量分析 `examples/analyze_all.py`

| 指标 | 结果 |
|---|---|
| 扫描图纸 | 97 张 |
| 识图成功 | **97 / 97** |
| 清单统计成功 | **97 / 97** |
| 含国标图框 | 81 |

图别分布：平面图 21 / 结构配筋 9 / 剖面 6 / 电气 6 / 机械零件 5 / 钢结构 4 / 立面 4 / 电子 3 / 防火分区 3 / 空调 3 / 横道 3 / 总平面 3 / 结构 3 / 排水 2 / 楼梯 2 / 节点大样 2 / 基础 2 / 给水 2 / 施工总平面 1 / 火灾报警 1 / 智能化 1 / 雨水 1 / 防排烟 1 / 基础平面 1 / 未识别 8

> 未识别 8 张全部是 `examples/out/` 下的**单特性 API 演示件**（dots / panel_* / style_* / rect_holes），按设计就没有图名与国标图层，判"未识别"是正确行为，非缺陷。

### 3.3 零回归确认

| 脚本 | 结果 |
|---|---|
| `verify_nl.py` | **46 / 46** |
| `verify_interfaces.py` | **21 / 21** |
| `verify_v114.py` | **60 / 60** |

---

## 四、交付物清单

| 文件 | 说明 |
|---|---|
| `scripts/drawing_reader.py` | 识图引擎（新增，约 800 行） |
| `scripts/bom.py` | 工程量清单统计（新增，约 560 行） |
| `scripts/construction_notes_v2.py` | 施工说明话术库 v2（新增） |
| `scripts/templates_electrical_v2.py` | 配电系统图 + 防雷接地图（新增） |
| `scripts/templates_hvac_v2.py` | 空调水系统图 + 风管平面图（新增） |
| `scripts/dxfkit.py` | 主链集成（追加 6 个导入块 + `SPECIALTY_TEMPLATES`） |
| `examples/verify_v114.py` | 新增能力验证（60 项） |
| `examples/analyze_all.py` | 全量 DXF 分析 |
| `examples/out_v114/` | 4 张新专业洞成品图 + BOM 样例 + 全量分析报告 |
| `SKILL.md` | 同步 v1.14.0（新增 5 个章节） |
| `功能表.md` | 同步 v1.14.0（新增 4 个章节） |

---

## 五、当前能力全景（十四层）

| # | 能力 | 状态 |
|---|---|---|
| 1 | 核心构建器 5 级链（DxfBuilder→GB→Review→Code→NL） | ✅ |
| 2 | 绘图原语（墙/门/窗/梯/标注/几何） | ✅ |
| 3 | 专业模板库（建筑/结构/机电/给排水/高级） | ✅ |
| 4 | 自然语言生成（19 类路由） | ✅ |
| 5 | 渲染导出（PNG/SVG/PDF/JPG/WEBP） | ✅ 中文零 tofu |
| 6 | 3D 导出（OBJ/STL/PLY） | ✅ |
| 7 | 图纸审查（六检查 + A~D 评级） | ✅ |
| 8 | 施工说明 + 规范引用（64 条 ×13 类） | ✅ |
| 9 | 图纸管理（编号/目录/图签/门窗表/材料表） | ✅ |
| 10 | 中文字体 + 48 国标图层 | ✅ |
| 11 | 接口层（CLI + REST API） | ✅ |
| 12 | **识图引擎**（图 → 结构化理解） | ✅ **新** |
| 13 | **工程量清单**（图 → 工程量表） | ✅ **新** |
| 14 | **施工说明 v2 + 电气暖通专业洞** | ✅ **新** |

---

## 六、遗留与下一步

**遗留（非缺陷）**：
- 8 张 API 演示件无图名，判"未识别"——设计如此
- 识图对**非本库生成**的第三方 DXF 识别率未验证（当前测试集全部为本库产物，规则已知）

**可选下一步**：
1. **标准图册**：补国标图集（16G101 平法等节点大样库）
2. **识图通用化**：拿外部真实 DWG/DXF 测试，评估对未知图纸的识别能力
3. **出图→算量→说明 一键流水线**：把三个模块串成单函数 `pipeline(text)`

---

**结论**：v1.14.0 完成了从"只会出图"到"出图 + 读图 + 算量 + 配说明"的能力闭环，四个新模块全部真实落地并通过 60 项验证、97 张全量分析、零回归。

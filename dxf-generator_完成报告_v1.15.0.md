# dxf-generator Skill 完成报告 v1.15.0

| 项 | 内容 |
|---|---|
| 技能名 | `dxf-generator` 图纸工坊 |
| 版本 | **v1.15.0**（v1.14.0 → v1.15.0） |
| 技术栈 | 纯 Python / 离线 / ezdxf 1.4.4（3D 无额外依赖） |
| 报告日期 | 2026-09-10 |
| 主题 | **DXF → 3D 体量挤出**：平面图变能转着看的立体模型 |
| 状态 | ✅ 落地，验证 37/37，端到端跑通 |

---

## 一、要解决的问题

此前"3D 导出"（`interfaces.DXFTo3D`）实际只是把闭合轮廓**平铺成 z=0 的三角薄片**——
没有厚度、没有层高、不能多楼层、无法预览。

本次补上真正的**挤出**：闭合轮廓 → 底面 + 顶面 + 侧面 → 可旋转的立体体量。

---

## 二、新增模块 `scripts/extrude3d.py`

| 组件 | 作用 |
|---|---|
| `ExtrudeParams` | 挤出参数（各图层高度、基准标高、法线翻转） |
| `ExtrudeBuilder` | 单层挤出：三类几何 → 闭合实体网格 |
| `MultiFloorBuilder` | 多楼层：按标高逐层堆叠 |
| `Mesh` | 索引式三角网格（顶点 + 三角面 + 包围盒） |
| `export_stl / export_obj` | 二进制 STL（带法线）/ 文本 OBJ |
| `export_viewer_html` | Three.js 独立 HTML，拖拽旋转 + 滚轮缩放 |
| `pair_parallel_lines` | **双线墙配对**（核心难点） |
| `rebuild_loops` | 线段闭环重建（兜底） |

### 图层高度表

墙体 3000 / 柱 3000 / 梁 500 / 板 200 / 基础 600 / 门窗 3000；
管道 200 / 设备 800 / 电线 100。
图框、标注、文字、轴线、填充、符号 → 自动跳过不挤出。

---

## 三、攻克的三个真问题

### 3.1 墙体挤不出来（最关键）

第一版跑出来只有 6 个柱子，**墙体全丢**。排查发现：

```
G_WALL 图层 18 条 LINE，端点度数分布 {1: 28, 2: 4}
→ 两条平行线各自悬空，端头没有封口线，根本不构成闭合环
```

**根因**：本库的 `wall_h` / `wall_v` 画的是**双线**（两条平行线各偏 120mm），
不是闭合多段线。

**解法**：新增 `pair_parallel_lines()` —— 把长度相近（±20%）、平行（±3°）、
间距 20~800mm 的两条线配对成一个矩形轮廓。

**效果**：轮廓数 6 → **27**（墙 9 + 窗 12 + 柱 6），双线配对命中 **21 对**。

### 3.2 STL 头字节数不对

`b"DXF Skill extrude3d"` 是 18 字节，补 60 个 `\x00` 只有 **78 字节**，
而 STL 规范要求头部精确 **80 字节**。验证脚本用 `84 + 50×三角数` 卡准长度时暴露。

**解法**：`.ljust(80, b"\x00")`。

### 3.3 三角化要支持凹多边形

扇形三角化对凹多边形会产生重叠面。改用 `ezdxf.math.triangulate`（earcut 算法），
不可用时回退扇形。

---

## 四、验证结果（`examples/verify_v115.py` · 37/37 PASS）

| 组 | 内容 | 结果 |
|---|---|---|
| [1] 轮廓提取 | 双线配对正确、不等长/超间距不误配、闭环重建 | PASS |
| [2] 单层挤出 | STL(体积 = 84+50n 精确)、OBJ、索引合法、高度 3000mm | PASS |
| [3] 多楼层 | 三层总高 9400mm 精确、180 三角面 | PASS |
| [4] HTML 预览 | Three.js + OrbitControls + Z-up 坐标 | PASS |
| [5] 主链集成 | dxfkit 导出 10 个新符号 | PASS |

### 端到端实测

```
一句话 "画一个三层住宅平面图"
  → ① 2D 施工图 201 实体
  → ② 3D 体量 324 三角面（墙 9 + 窗 12 + 柱 6）
  → ③ 可旋转 HTML 页面
全链路通
```

### 实测数据

| 样本 | 轮廓 | 三角面 | 包围盒 |
|---|---|---|---|
| 建施-01 一层平面图 | 27（墙9/窗12/柱6） | 324 | 12400 × 8400 × 3000 mm |
| 别墅 1F+2F+3F | 三层叠加 | 180 | 2000 × 3200 × 9400 mm |

---

## 五、交付物

| 文件 | 说明 |
|---|---|
| `scripts/extrude3d.py` | 挤出引擎（新增，约 620 行） |
| `scripts/dxfkit.py` | 主链集成（追加 extrude3d 导入块） |
| `examples/verify_v115.py` | 验证脚本（37 项） |
| `examples/out_v115/` | STL / OBJ / HTML 产物 |
| `SKILL.md` | 同步 v1.15.0 |
| `功能表.md` | 同步 v1.15.0 |

### 桌面交付（可直接双击查看）

- `一层平面_3D体量.html` / `.stl`
- `别墅三层_3D体量.html` / `.stl`
- `一句话生成3D体量.html`

---

## 六、诚实的边界（重要）

| 能做到 | 做不到 |
|---|---|
| 有厚度、有层高的体块 | ❌ 工程级 BIM（精确构件属性） |
| 多楼层按标高叠加 | ❌ 门窗洞口自动开洞（窗是实心块） |
| 浏览器拖拽旋转查看 | ❌ 楼层间构件对齐校验 |
| 一句话 → 2D → 3D 全链路 | ❌ 替代 Blender / SketchUp 精修 |

**定位**：这是**可视化体量模型**，用于方案演示、概念沟通、网页展示。
要精确到门窗洞口和结构配筋，仍需在专业软件里精修。

---

## 七、能力全景（十五层）

| # | 能力 | 版本 |
|---|---|---|
| 1–11 | 构建器链 / 绘图原语 / 专业模板 / NL 生成 / 渲染 / 3D 导出 / 审查 / 说明 / 图纸管理 / 字体标准 / 接口层 | v1.0–1.13 |
| 12 | 识图引擎（图 → 结构化理解） | v1.14.0 |
| 13 | 工程量清单（图 → 工程量表） | v1.14.0 |
| 14 | 施工说明 v2 + 电气暖通专业洞 | v1.14.0 |
| 15 | **DXF → 3D 体量挤出（+ 浏览器预览）** | **v1.15.0** |

---

**结论**：v1.15.0 把"平面图"升级为"能转着看的立体体量"，
解决了本库墙体为双线 LINE 导致的挤出难题，全链路（一句话→2D→3D→预览）跑通，验证 37/37。

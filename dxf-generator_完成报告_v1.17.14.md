# dxf-generator 完成报告 · v1.17.14（P0-1 修法②③ 实证驳回 + 回归门禁）

> 日期：2026-09-12 ｜ 路线：B 路线识图评测集（benchmarks/）｜ 唯一可信分母：external n=26
> 核心结论：**火灾报警图误判簇已被 v1.17.13 窄①修好（5→0）；主人后续给的 ②③ 实测会复活该簇（0→5），属 ❌误伤，已驳回。新增回归门禁 `verify_fire_alarm_fix.py`。**

---

## 一、背景与任务

- P0-1 唯一未动核心靶子 = 火灾报警图误判簇（5 张，占 16 处自信答错 31%）。
- v1.17.13 已用**窄①**（文本级图例排除 `图例` + 图名级关键词匹配）修好：火灾报警误判 5→0、严格正确 3→4、认错 19→17。
- 主人在此之后又给三件套，要求"落①②③、跑 before/after、火灾报警 5→1-2 且严格正确不降即成功"：
  - 用户版①：按**图层名**过滤图例层（`LEGEND_LAYER_KEYWORDS` 含 标注/说明/标题/图框/材料表/门窗表/轴号/dim）；
  - ②：火灾报警图关键词 `("火灾报警","火警")` → 17 个；
  - ③：火灾报警图图层信号 `("E-FIRE","E-ALARM","FIRE","ALARM","消防","报警","火灾")`。

---

## 二、先测用户版①（过度过滤）

`_diag_user_filter.py` 原样套用 `LEGEND_LAYER_KEYWORDS` 测 26 张 external：

- 共 18895 文本，**3250 (17%)** 被"图例层"过滤，峰值 **29%**（19 别墅_273结构）。
- 真实图层 `L标注` / `A建筑标注` / `0-图框、标题、说明` 全中 → 房间名/标高/配筋标注被删。
- 其中"文字命中"图例表过滤仅 66/18895 = 0.3%（无害）；**有害的是图层名那部分**。
- 判定：与"图例区不参与打分"初衷相悖，且与窄①功能重叠 → **不替换窄①**。

---

## 三、②③ 上机 before/after（驳回依据）

| 指标 | BEFORE（窄①） | AFTER（窄①+②③） | 判定 |
|---|---|---|---|
| ★认错 | 17/26 (65.4%) | **18/26 (69.2%)** | ❌ 升 |
| 严格正确 | 4/26 | 4/26 | ➖ 持平 |
| 宽松正确 | 2/26 | 1/26 | ❌ 降 |
| **火灾报警图误判** | **0 张** ✅ | **5 张** ❌ | ❌ 复活 |

逐张退化（AFTER 把哪些图喂成火灾报警图）：
| 样本 | 真值 | BEFORE 识别 | AFTER 识别 |
|---|---|---|---|
| 03 1F | 平面图 | 排水系统图 | 火灾报警图 |
| 04 -1F | 平面图 | 总平面图(⚠️) | 火灾报警图 |
| 05 2F | 平面图 | 排水系统图 | 火灾报警图 |
| 06 3F | 平面图 | 排水系统图 | 火灾报警图 |
| 牧马中央空调、新风、地暖施工图 | 暖通图 | 节点大样图 | 火灾报警图 |

- 单独回退③复测确认：**仅靠②就复活簇**（平面图含"探测器/总线/手报/声光"等设备文字，被②宽关键词子串命中）。
- ③ 的图层子串 `消防/报警/火灾` 同理会命中消防系统图/平面图图层，只会更糟。
- 根因：当前识图匹配是**子串计数**；②的"探测器/总线"是设备名非图名、③的"消防/报警/火灾"是图层子串；26 张里**无一张真火灾报警图可增益**，纯亏不赚。

---

## 四、判定（主人定的口径）

1. ❌ **误伤**：火灾报警图误判 0→5（复活簇）——正是主人亲自提示的失败模式；
2. 认错 17→18（升）、宽松 2→1（降）。
3. **结论：驳回 ②③，保留窄①。** 若日后出现真火灾报警图样本，再用更稳机制重测：
   - ② 限"图名级"token（仿 `_type_keywords_in_text` 的后缀约束），不要裸设备名；
   - ③ 用**具体图层前缀**（如 `E-FIRE-*`）而非子串 `消防/报警/火灾`；
   - external≥30 前不调任何阈值（n 仍小，避免过拟合）。

---

## 五、新增回归门禁

- `benchmarks/verify_fire_alarm_fix.py`：复用 `read()` 真实流水线 + eval 的 strict/loose/wrong 口径；
  验收 **火灾报警误判≤2 / 严格正确≥3 / 认错≤19**，达标退 0、否则退 1（可挂 CI/提交门禁）。
- 当前态（窄①）实跑：**✅ 达标** —— 火灾报警误判 0 / 严格正确 4 / 认错 17（=14 自信答错 + 3 空答）。

---

## 六、交付物 / 改了什么

- 代码：`scripts/drawing_reader.py` **零改动**（已为 v1.17.13 窄①，②③ 已回退，无回归）。
- 新增 `benchmarks/verify_fire_alarm_fix.py`（回归门禁）。
- 新增 `benchmarks/eval_②③_rejected.md`（②③ before/after 全表 + 根因）。
- 新增 `benchmarks/_diag_user_filter.py`（用户版①过度过滤测量）。
- 文档：`SKILL.md` → v1.17.14；`功能表.md` → §二十七。
- 全量 `verify_all.py`：21 套件 · 575 断言 · 0 失败（drawing_reader 零回归，已确认）。

---

## 七、复现命令

```bash
python benchmarks/verify_fire_alarm_fix.py     # 当前态验收（应 ✅ 达标）
python benchmarks/_diag_user_filter.py          # 用户版①过度过滤测量（17%）
python benchmarks/eval.py --external-only       # 真实识别能力（认错 17/26）
```

**一句话总结**：用 before/after 数据拦住了"看似更全、实则复活误判簇"的②③；火灾报警图修复以窄①收口并加门禁防回归。

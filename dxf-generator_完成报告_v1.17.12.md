# dxf-generator 完成报告 · v1.17.12

> 日期：2026-09-12 ｜ 模式：Agent ｜ 关联：v1.17.11（增强2+增强3）收口后的补强

## 一、本版目标
v1.17.11 完成后，主人就"人工/机械单价是否接入预算模型"拍板 **选项 C**：
- 不动预算模型、不接任何计算；
- 只在 `budget_from_bom.md` 报告里把"人工费/机械费的隐性假设"显式打印两行。

纯报告层增强，零回归。

## 二、改动清单
| 文件 | 改动 |
|---|---|
| `scripts/budget.py` | `BudgetGenerator.to_markdown`：在「费用组成」表后、`⚠️` 警告前追加「隐性假设说明」两行（人工费=面积×650、机械费=材料费×4%，均带占比/反算口径） |
| `SKILL.md` | `version: 1.17.11` → `1.17.12` |
| `功能表.md` | 头部 → v1.17.12；新增 §二十五（选项 C 收口） |

**未动**：`LABOR_PER_SQM` / `EQUIPMENT_RATE` 常量、`generate()` 计算逻辑、`Budget` 数据类、pipeline 任何步骤。

## 三、★ 实测验证
1. `examples/validate_pipeline_budget.py`
   - 用例 A（`budget_area=288, floors=2`）：成功生成 `budget_from_bom.md` + `coverage.md`，总造价 **219,319 元**；md 内含两行假设说明，数字自洽（人工费 160×650=104,000 / 47.4%；机械费 69,712×4%=2,788）。
   - 用例 B（`budget_area=0`）：干净跳过，零回归。
2. 全量 `examples/verify_all.py`：**21 套件 · 575 断言 · 0 失败**（NL 流水线 57/57 仍稳，确认对 v1.17.11 的 step-slot 修复无回退）。

## 四、诚实边界（必读）
- "反算 ≈2.1 工日/m²" 仅按经验工日单价 310 元/工日估算，**工日数未经定额校准**。
- `LABOR_PER_SQM=650` 可能系统性偏低（典型住宅 2.5–4 工日/m² × 310 ≈ 775–1240 元/m²）。本版**只暴露、不修正**，待主人日后拍板是否上调。
- 机械费 4% 固定比例、未按工种区分台班；占比 1.3%，选项 B（按台班）性价比差，故维持。
- 报告仍**非招标/结算依据**，与 §23.5 一致。

## 五、提交
- 提交 + 推送至 `github.com/liubin8199-1/dxf-generator`，`ls-remote` 校验远端==本地。
- 人读交付物同步桌面一份（本完成报告 + 功能表.md 已含 §二十五）。

---
**结论**：预算报告里"拍脑袋的部分"现在一眼可见——使用者能立刻区分"图纸几何推导的量"与"经验常数拍的价"。

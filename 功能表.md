# dxf-generator 功能表（v1.17.11）

> 纯 Python / 离线 / ezdxf 驱动，把自然语言或参数化调用转成 GB 标准 DXF 施工图。
> 支持：文档/模型空间/图元/图层/保存/图纸空间 + 五大能力（导入修改·参数化模板·批量·样式·NL 分发）+ 渲染/3D/审查/说明/规范/管理全套。

## 一、核心构建器（Builder 链式继承）

| 构建器 | 能力 | 关键方法 |
|---|---|---|
| `DxfBuilder` | 基础 DXF 构建（模型空间 1:1、图元、图层、保存） | `wall_h/v`、`door`、`window`、`stair`、`dim_h/v`、`text`、`circle`、`rect`、`add_polyline`、`save` |
| `GBDxfBuilder` | 在 DxfBuilder 基础上 + 国标图框/标题栏/1:100 视口 | `add_gb_sheet(paper_size, title_data, …)`、`add_gb_border`、`add_gb_axis`、`add_gb_dimension` |
| `ReviewAwareDxfBuilder` | + 六项自动审查 | `.review()` 出 A~D 评级 |
| `CodeAwareDxfBuilder` | + 规范引用绘制 | 把 GB/JGJ 规范清单画进图纸 |
| `NLAwareDxfBuilder` | + 自然语言入口（NL→Review→Code→GB 完整链） | `generate_from_text(text, file, add_sheet=True)`、`parse_text(text)` |

## 二、绘图原语（DxfBuilder 基础图元）

| 类别 | 方法 |
|---|---|
| 墙体/门窗 | `wall_h` `wall_v`（含门窗洞口）、`door` `window` `add_rectangle` |
| 楼梯/标高 | `stair` `elevation_mark` `compass`（指北针） |
| 标注/文字 | `dim_h` `dim_v`（尺寸）、`text`（中文自动绑 GB_CHINESE） |
| 几何 | `circle` `arc` `ellipse` `add_polyline` `add_line` `bezier` `spiral` `spline` |

## 三、专业模板库（一行调用出图）

| 模板库 | 模块 | 提供的图纸 / 函数 |
|---|---|---|
| 建筑专业 | `templates_arch` | 立面图 `elevation`、剖面图 `section`、节点大样 `detail`、楼梯详图 `stair_detail` |
| 结构专业 | `templates_struct` | 钢筋符号 `bar/bar_label/bar_mark/hook`、柱配筋 `column_rebar`、板配筋 `slab_rebar`、基础 `foundation`、楼梯配筋 `stair_rebar`、**梁配筋 `beam_rebar`**（上下通长筋+支座负筋+箍筋加密+1-1/2-2 断面+钢筋表） |
| 机电专业 | `templates_mep` | 电气符号、电气图例 `electrical_legend`、照明平面 `lighting_plan` |
| 给排水（v1.13 真模板） | `templates_plumbing` | 卫生间大样 `bathroom_detail`、给水系统 `water_supply_system`、排水系统 `drainage_system`、消防喷淋 `fire_fighting_plan(mode='sprinkler')`、**消火栓系统 `fire_fighting_plan(mode='hydrant')`** |
| 高级专业模板 | `templates_advanced` | 总平面图、防火分区·消防疏散图、空调系统图、防排烟系统图、雨水系统图、火灾报警系统图、智能化系统图、施工进度横道图、施工总平面图、钢结构详图（钢柱/钢梁） |
| 高级几何 | `geomkit` | 椭圆、参数化齿轮、螺旋线、贝塞尔曲线 |
| 建筑标准构件 | `archkit` | 轴网、双线墙、标准门窗、楼梯、标高、指北针、柱梁、图框标题栏、标准户型生成器 |
| 造价预算 | `budget` | 工程量→造价→采购清单 → CSV/JSON/Excel/Markdown |

## 四、自然语言生成（NL → DXF）

| 能力 | 说明 |
|---|---|
| 入口 | `demo_natural_language(text, file)` 或 `NLAwareDxfBuilder.generate_from_text(text, file, add_sheet=True)` |
| 识别类型 | **19 类**：floor_plan / elevation / section / stair / detail / site_plan / fire_safety / hvac / fire_alarm / intelligent / smoke_exhaust / rain_water / electrical / plumbing / structural / steel / foundation / schedule / construction_site |
| 关键词路由 | 复合优先层（消防喷淋/喷淋/消火栓/给水/排水/供水…）→ 对应分支；弱匹配层（卫生间/浴室/卫浴/…）兜底 |
| 自动追加 | 生成后自动补「施工说明」+「规范引用」+「国标图框」 |
| 中文 | 解析与出图全程中文零 tofu（字体自动绑定 GB_CHINESE） |

## 五、渲染与导出

| 能力 | 接口 | 格式 |
|---|---|---|
| 栅格/矢量渲染 | `DXFToImage().export(dxf, fmt, cfg)` | **PNG / SVG / PDF / JPG / WEBP**（ezdxf.addons.drawing 全实体渲染 + CJK 手绘文本，中文零 tofu） |
| 3D 导出 | `DXFTo3D().export(dxf, fmt, cfg)` | **OBJ / STL / PLY** |

## 六、图纸审查系统

| 检查项 | 说明 |
|---|---|
| 六项检查 | 图层 / 图框 / 文字 / 尺寸 / 完整性 / 规范引用 |
| 评级 | A~D 级（A 优秀 / B 良好 / C 合格 / D 不合格），输出文本报告 + 写入 DXF |
| 批量 | `BatchReviewer` 汇总；`ReviewAwareDxfBuilder.review()` 一行调用 |
| 负面夹具 | `out_adv/review_缺图框坏图.dxf` 用于验证审查器能正确判缺框 |

## 七、施工说明 & 规范引用

| 模块 | 能力 |
|---|---|
| `construction_notes` | 按 GB 系列自动生成各专业施工说明（一般/土方/基础/主体/砌体/屋面/装饰/给排水/电气/暖通/消防/安全），一行写入图纸空间或模型空间 |
| `construction_codes` | 内置 **64 条 ×13 类** GB/JGJ 规范库，按图纸类型/专业自动匹配强条/推荐/参考，`CodeAwareDxfBuilder` 一行把清单画进图 |

## 八、图纸管理全套

| 模块 | 能力 |
|---|---|
| `drawing_management` | 图纸编号系统 / 目录 / 图签+会签栏 / 门窗表 / 材料做法表 / 结构设计说明 / 设备材料表 / 工程量清单；`CompleteDrawingManager` 一键出全部表格 |

## 九、中文字体与国标标准

| 模块 | 能力 |
|---|---|
| `font_manager` | 自动注册 GB_CHINESE / GB_TITLE / GB_MULTILINE 样式（gbenor.shx + gbcbig.shx），所有 CJK 实体自动绑定，杜绝「中文 ??」乱码 |
| `auto_fix_cjk` | 修复脚本：`auto_fix_dxf_styles()` 强制绑样式 + `verify_glyph_coverage()` FT2Font 逐字缺字校验 |
| `gb_standards` | **48 个国标图层**、标准线型/线宽、符号/文字/图框标准；`GBDxfBuilder` 出全套施工图 |

## 十、示例与验证脚本

| 脚本 | 作用 |
|---|---|
| `examples/nl_demo.py` | 10 个 NL 一句话生成示例 |
| `examples/pro_templates.py` | 12 张专业模板成品（已带国标图框） |
| `examples/villa_floors.py` | 别墅三层独立成图（已带框） |
| `examples/extensions_showcase.py` | geomkit+archkit+budget 三大模块跑通（已带框） |
| `examples/interfaces_demo.py` `verify_pipeline.py` | 接口层 / 端到端一键验证（走已修复 NL 链） |
| `examples/regen_v113.py` | 用修复后完整链重生成 out_v113 |
| `examples/review_all.py` | 全量六项审查，出 A~D 评级报告 |
| `examples/verify_pipeline.py` | 4 步 e2e：NL→DXF→PNG→结构/glyph 校验 |

---

## 十一、识图引擎（v1.14.0 新增 · 图 → 结构化理解）

| 能力 | 说明 |
|---|---|
| 入口 | `read('x.dxf')` / `describe('x.dxf')` / `DrawingReader` |
| 识别内容 | 元信息 / 图层专业归类 / 实体分类 / 文字 / 块引用 / 尺寸标注 / 几何量算 |
| 图别推断 | 三信号源加权：文件名 0.8 > 图内文字 0.7 > 图层特征 0.4 |
| 完整性体检 | 图框 / 标题栏 / 文字层 / 标注层 / 中文样式 / 量纲一致性 |
| 输出 | `to_dict()` / `to_json()` / `to_markdown()` |

## 十二、工程量清单统计（v1.14.0 新增 · 图 → 工程量表）

| 能力 | 说明 |
|---|---|
| 入口 | `generate_bom('x.dxf', 'out/')` / `BOMCalculator` / `batch_bom` |
| 归并规则 | 28 类国标类别（墙体/门窗/钢筋/梁柱板/管道/电气/标注…） |
| 计量 | 线状→长度(m)、闭合→面积(m²)、块/文字→个数 |
| 导出 | CSV / JSON / **Excel**（含分类汇总页）/ Markdown |
| 辅助 | `classify_layer(name)` 图层归类，可单独调用 |

## 十三、施工说明 v2（v1.14.0 新增 · 按图纸类型匹配）

| 能力 | 说明 |
|---|---|
| 库规模 | **28 类图纸 / 143 条条款 / 43 部规范** |
| 入口 | `get_construction_notes('beam_rebar')` / `ProfessionalPhraseLibrary` |
| 识图联动 | `match_by_drawing_type(识图结果)` → 自动匹配话术 key |
| 专业覆盖 | 建筑 / 结构 / 给排水 / 暖通 / 电气 / 消防 / 施工管理 |

## 十四、电气 / 暖通专业洞（v1.14.0 补齐）

| 模板 | 函数 | 规模 |
|---|---|---|
| 配电系统图（单线图） | `dist_power_system(DistributionParams)` | 113 实体 |
| 防雷接地图 | `lightning_grounding(LightningParams)` | 46 实体 |
| 空调水系统图 | `chilled_water_system(ChilledWaterParams)` | 80 实体 |
| 风管平面图 | `duct_plan(DuctPlanParams)` | 69 实体 |

统一注册表：`dxfkit.SPECIALTY_TEMPLATES`。

---

**当前状态（v1.14.0）**：在 v1.13.1（生成引擎修复）基础上，补齐**识图 / 清单统计 / 施工说明 v2 / 电气暖通专业洞**四大板块，形成"出图 → 识图 → 算量 → 说明"完整闭环。
- 验证：`examples/verify_v114.py` **60/60 PASS**
- 全量：`examples/analyze_all.py` 对 **97 张 DXF** 识图 97/97、清单 97/97
- 报告：`examples/out_v114/_full_analysis.json` / `_full_analysis.md`
- 遗留：8 张 `out/` 单特性 API 演示件无图名，按设计判"未识别"（非缺陷）

---

## 十五、DXF → 3D 体量挤出（v1.15.0 新增 · 平面图变立体模型）

| 能力 | 说明 |
|---|---|
| 入口 | `ExtrudeBuilder(ExtrudeParams)` / `MultiFloorBuilder` |
| 挤出原理 | 闭合轮廓 → 底面 + 顶面 + 侧面（**真体块**，非平面薄片） |
| 三类几何 | 闭合 LWPOLYLINE / CIRCLE / **双线 LINE（配对成矩形）** |
| 多楼层 | `MultiFloorBuilder(floor_height, slab_thickness).build([(dxf, base_z), …])` |
| 图层高度 | 墙 3000 / 柱 3000 / 梁 500 / 板 200 / 基础 600（`DEFAULT_LAYER_HEIGHTS`） |
| 自动跳过 | 图框 / 标注 / 文字 / 轴线 / 填充 / 符号（`SKIP_LAYER_PREFIXES`） |
| 导出 | 二进制 STL（80 字节头 + 正确法线）/ 文本 OBJ（索引面） |
| **预览** | `export_viewer_html()` 生成独立 HTML，Three.js + 拖拽旋转 + 滚轮缩放 |

**核心难点（已解决）**：本库墙体是"双线 LINE"（两条平行线、端点不闭合，度数 `{1:28, 2:4}`），
必须用 `pair_parallel_lines()` 配对成矩形轮廓才能挤出——否则只能挤出柱子，墙体全丢。

**诚实边界**：产物是**体量模型**（有厚度/层高/可转），**非工程级 BIM**；
门窗洞口不自动开洞；多层为机械叠加，无构件对齐校验。

---

**当前状态（v1.17.0）**：在 v1.16.0（一键流水线）基础上，把**施工说明搬进图纸空间说明栏**，
修掉**从 v1.14 就存在的视口 bug**，并新增**成品图幅渲染**与**说明自动分页**。
- 验证：`verify_layout_notes.py`（新）**69/69**、`verify_v114.py` **60/60**、
  `verify_v115.py` **37/37**、`verify_pipeline_v115.py` **57/57**、
  `verify_nl.py` **46/46**、`verify_interfaces.py` **21/21**、
  `verify_pipeline.py`（v1.12 渲染）**4/4** → 合计 **294 项**
- 端到端：`pipeline("12x8米三层住宅平面图带客厅厨房卧室")` → 15 个产物（含 2 张成品图幅预览）

---

## 十六、一键流水线（v1.16.0 新增 · 一句话 → 完整交付包）

`scripts/pipeline.py` —— 把已有能力串成一条链，一句话跑完八个步骤。

```
一句话
  ↓ [1] 出图   drawing.dxf（语义命名）        NLAwareDxfBuilder.generate_from_text
  ↓ [2] 识图   understanding.json/.md         DrawingReader
  ↓ [3] 算量   bom/bom.{csv,json,xlsx,md}     generate_bom
  ↓ [4] 说明   construction_notes.md          get_construction_notes（按识图结果匹配）
  ↓ [4.5] 报价（图纸驱动）budget_from_bom.md + coverage  bom_to_budget.estimate_from_bom（bom 几何量→定额量→预算）※ budget_area>0 才触发
  ↓ [5] 审查   review.md（A~D 级）            DrawingReviewer
  ↓ [6] 渲染   render.png（可选，matplotlib） DXFToImage
  ↓ [7] 3D     model.{stl,obj,html}           extrude3d.ExtrudeBuilder
  ↓ [8] 汇总   pipeline_report.{txt,json} + manifest.json
```

| 入口 | 说明 |
|---|---|
| `pipeline(text, out_dir, **kwargs)` | 一句话跑全链，kwarg 覆盖配置 |
| `pipeline_batch(texts, root)` | 批量，每条一个子目录 |
| `Pipeline(PipelineConfig(...)).run(text)` | 完整控制 |
| CLI | `python pipeline.py "12x8米住宅平面图" -o out/ [--render] [--no-review] [--no-3d]` |

**设计要点**：
1. **识图前置到算量/说明之前**——施工说明的话术要按识图结果匹配（比 NL 图别更贴近实际图形）。
2. 除"出图"外，**任何一步失败都不中断**，失败写进该步状态，最后汇总列出。
3. 出图文件名用**输入语义命名**（不再统一叫 `drawing.dxf`）——识图里"文件名"权重最高(0.8)，
   固定名字等于自废武功。
4. 步骤 1~5 + 3D 默认开（3D 现在是纯 Python，快）；**渲染默认关**（300dpi 约 5~7 秒）。

**产物一致性**：`manifest.json` 记录每个产物的相对路径与字节数，便于整包归档交付。

### 验证（`examples/verify_pipeline_v115.py` · 55 项全过）

| 组 | 内容 |
|---|---|
| [1] 单条全链 | 8 步槽位齐全、四处产物、报告可回读 |
| [2] 批量 | 5 种图别全成功、专业有区分度 |
| [3] 配置变体 | 只出图 / 裁剪 BOM 格式 / 关 3D / 不加图框 |
| [4] 产物完整性 | manifest 逐项落地校验、关键产物存在 |
| [5] 主链集成 | dxfkit 导出 5 个新符号 |
| [6] 字体体检 + 渲染 | 各字号出字、未选点阵位图字体、PNG 落地 |

---

## 十七、渲染层两个老 bug 的修复（v1.16.0）

### 17.1 中文"静默丢字"（严重 · 影响所有渲染）

`SimSun`（`simsun.ttc`）含**点阵位图**，matplotlib/Agg 在约 **5~6pt 的字号带**把整行中文
渲染成**空白**（黑像素 = 0），ASCII 正常、且**不抛任何异常**。施工说明那类密排小字正好落在
该字号带 → 中文凭空消失，肉眼极难发现。

实测各字号黑像素（字符串"本工程图纸尺寸"）：

| 字体 | 3pt | 4pt | 5pt | 6pt | 7pt | 8pt | 10pt |
|---|---|---|---|---|---|---|---|
| **simsun.ttc** | 20 | 92 | **0** | **0** | 331 | 567 | 885 |
| msyh.ttc | 121 | 210 | 426 | 686 | 745 | 1088 | 1463 |
| simhei.ttf | 120 | 213 | 360 | 535 | 602 | 891 | 1434 |

**修复**：
- `_MPL_CJK_CANDIDATES` 把 SimSun 从首位**降到末位兜底**，优先 msyh / simhei / Deng / simkai / simfang
- 新增体检函数 `interfaces.check_cjk_font_sizes()`，把"静默丢字"变成**可断言检查**（回归防线）

### 17.2 文字尺寸失配（说明栏糊成一团）

`_to_raster` 里字号换算原用 `fig_h`（整图高度），但 `set_aspect('equal', adjustable='box')`
会把坐标轴盒子**缩小**，真实数据→英寸比例小于 `fig_h/range` → 字号被整体放大（实测 2.3 倍），
密排行距被吃掉，看起来糊成一团。

**修复**：先 `fig.canvas.draw()` 完成布局，再量 **实际坐标轴高度**（`ax.get_window_extent()`）
来算 `pts_per_unit`。

### 17.3 说明/规范落点（NL 自动追加）

原行为：施工说明按 1:100 放大后画在 `(23200, 28300)`，但 `width`（换行宽度）**不参与缩放**
→ 字高 250mm 却按 175mm 换行，**每行一个字**竖着糊成一列；执行规范更糟，默认 `(0,0)`
**直接压在图形上**。

**修复**：`natural_language_engine.generate()` 先量图形包围盒（`ezdxf.bbox.extents`），
把两段文字块放到**图形右侧空白区**，`width`/行距/字高**按同一比例缩放**，规范清单接在说明下方。

### 17.4 电气图例 `'str' object is not callable`（老 bug）

`electrical_legend()` 要的是 `(名称, 可调用对象, 说明)` 三元组，而 NL 引擎传的是
`{'sym': 'sym_lamp_ceiling'}` **字符串** → 任何电气类 NL 请求都直接报错（电气分支从未跑通）。

**修复**：`templates_mep.normalize_legend_item()` 兼容三元组与字典两种写法，
并新增 `_SYMBOL_REGISTRY`（符号名 → 绘制适配器）自动解析字符串符号名。

---

## 十八、施工说明移入图纸空间说明栏 + 视口修复 + 成品图幅渲染（v1.17.0）

v1.16.0 的遗留问题：**施工说明画在模型空间**，按 1:100 放大后把模型空间撑到
**34500mm 高**（一根巨型文字柱）——既不符合国标"模型 1:1、说明进图纸空间"的做法，
也让模型空间渲染出来的图形缩成角落一小块。本章是把它彻底搬回去，顺带挖出并修掉
一个**从 v1.14 就存在、一直静默**的视口 bug。

### 18.1 说明栏几何：是算出来的，不是拍出来的

`BorderStandard.paper_notes_rect(paper_size)` 按国标图框几何推导（图纸毫米）：

- `add_gb_sheet` 的视口宽 = 内框宽 − 标题栏宽(180) − 5，且视口**左对齐**内框
  → 内框右侧天然空出一条竖带；
- 标题栏（180×56）只占这条竖带的**底部一角** → 说明栏 = 竖带 ∩ 标题栏之上。

实测结果：

| 图幅 | 内框 | 视口 | **说明栏** |
|---|---|---|---|
| A3 | 25..410 × 10..287 | 200×272 | **(230, 71, 180, 216)** |
| A4 | 25..287 × 10..200 | 77×185 | (107, 71, 180, 129) |
| A2 | 25..584 × 10..410 | 374×395 | (404, 71, 180, 339) |

**关键结论：不需要压缩视口**——A3 右侧本来就有 180×216mm 的空白，直接可用。

### 18.2 ⚠️ 视口 bug：`add_viewport` 第 4 参数不是比例

```python
Layout.add_viewport(center, size, view_center_point, view_height)
#                                                    ^^^^^^^^^^^ 模型单位的高度！
```

老代码 `add_gb_sheet(..., scale=1/100)` 把 `scale` **直接**当第 4 参传进去
→ `view_height = 0.01mm` → 视口只看到模型里 0.01mm 高的切片。
证据：`get_modelspace_limits()` 退化成 **(5999.996, 3999.995)–(6000.004, 4000.005)**
一个点，`get_scale()` 返回 **27200**（应为 0.01 = 1:100）。

因为以前**从不渲染图纸空间**（`_to_raster` 只画 msp），这个错误一直没暴露。

**修复**：`view_height = 视口高(纸mm) / scale`，并新增

| 新 API | 作用 |
|---|---|
| `BorderStandard.viewport_size(paper)` | 视口尺寸，与 `add_gb_sheet` **同源**（避免两处各算一遍） |
| `BorderStandard.fit_scale(w, h, paper)` | 按模型包围盒挑**标准比例**（1/2/5/10/20/25/50/100/…），取能装下的最大比例 |
| `BorderStandard.scale_label(scale)` | `0.01 → "1:100"` |
| `add_gb_sheet(..., view_center=None, with_viewport=True)` | 传 None 自动取模型包围盒中心；续页可无视口 |

NL 出图现在**自动定比例 + 自动居中**：`12x8米住宅` 实测模型 16621×17550mm → 自动 1:100，
标题栏比例同步写 `1:100`。

### 18.3 排版三件事：可读优先、分页、标题不落单

| 机制 | 说明 |
|---|---|
| **可读优先** | 说明正文 **2.5mm** / 行距 3.8mm；执行规范基数 5.0（最小一级 2.5mm） |
| **自动分页** | 一页装不下 → 自动新建 `GB_A3_说明2` 图幅（同图框/标题栏、**无视口**） |
| **标题不落单** | 切页时若本页末尾是「三、引用规范」这类标题，挪到下一页开头 |
| `auto_fit` | 仍保留：给定 `max_height` 时迭代压缩行距/字高，但**下限 2.5mm**——宁可分页也不缩到读不出 |

> 踩过的坑：最初用 `auto_fit` 把整套说明硬压进一页，结果字高掉到 **1.8mm**
> ——"页面不溢出、但印出来读不出"，是**假达标**。改为分页后全部 ≥2.5mm。

### 18.4 成品图幅渲染（`space='paper'`）

```python
DXFToImage().export("x.dxf", "png", {"space": "paper", "layout": "GB_A3"})
```

⚠️ **第二个坑**：ezdxf 的 `_draw_viewports` 会把 **status==1** 的视口当成
"正在编辑的活动视口"**跳过**（它认为整张布局已被该视口代表）→ 图纸空间只画出图框、
视口里的图形一片空白。渲染时把 status 临时抬到 **2** 就正常；
**只读入不写盘，磁盘上的 DXF 不受影响**。

- `_pick_paper_layout(doc, name)`：优先带 VIEWPORT 的布局，找不到退到最后一个非 Model 布局
- 流水线新增 `[6b]`：`do_render` 时对每个说明图幅各渲染一张 `sheet.png` / `sheet2.png`

### 18.5 验证（`examples/verify_layout_notes.py` · 69 项全过）

| 组 | 内容 |
|---|---|
| [1] 说明栏几何 | A3/A4/A2/A1 四项：在内框内、不压视口、不压标题栏、面积可用 |
| [2] 视口换算 | `view_height`/`get_scale`/`modelspace_limits` 三者自洽；`fit_scale` 单调 |
| [3] 说明落点 | 进 layout；模型空间 bbox 高度 < 25000mm（旧 34500+） |
| [4] 分页 | 页数=布局数；字高全 ≥2.5mm；续页无视口 |
| [5] 成品图幅渲染 | 布局选择器 4 项 + 两空间渲染 + 体积 |
| [6] 向后兼容 | `add_sheet=False` / `notes_in_layout=False` 仍走模型空间 |
| [7] auto_fit | 生效/不跌破下限/下限处不硬缩/关掉时如实溢出 |
| [8] 主链集成 | pipeline 报告 + `to_dict` + `manifest` 带落点/页数/图幅 |

**回归**：`verify_nl` 46/46、`verify_interfaces` 21/21、`verify_v114` 60/60、
`verify_v115` 37/37、`verify_pipeline_v115` **57/57**、`verify_pipeline` 4/4 —— 全绿。

> `verify_pipeline_v115` 的「PNG 体积 >50KB」断言按新行为**改了判据**：
> 说明书搬走后模型空间渲染从 ~377KB 降到 ~48KB（图形反而占满画面），
> 这是变好不是回归；改为「体积区间 + 模型 bbox 高度 <25000mm」这类**内容相关**判据。

---

## 十九、图纸空间布局选取规则 + 识图读取通用化（v1.17.1）

### 19.1 布局选取规则（写死，已进正式文档）

渲染 `space='paper'` 时按固定优先级挑布局：

| 顺序 | 条件 | 结果 |
|---|---|---|
| 1 | 显式给 `layout='名字'` 且存在 | 用它 |
| 2 | 显式给名字但找不到 | `None` → 回退模型空间渲染 |
| 3 | 未给名字，存在**带 VIEWPORT** 的布局 | 取第一个带视口的 |
| 4 | 未给名字，无任何带视口布局 | 取**最后一个非 Model 布局** |
| 5 | 无任何非 Model 布局 | `None` |

- `doc.layouts` 第一个永远是 `'Model'`，**不是**图纸空间，必须排除。
- 判据是"**有没有 VIEWPORT 实体**"，不是布局名 —— 图纸空间可以有多个
  （分页说明续页就是），靠名字猜会挑错。

### 19.2 ⚠️ 视口 `status==1` 会被 ezdxf 渲染器丢掉（已核对源码）

**出处**（ezdxf 1.4.4 逐行核对）：`ezdxf/addons/drawing/frontend.py :: _draw_viewports()`

```python
viewports.sort(key=lambda e: e.dxf.status)
viewports = [vp for vp in viewports if vp.dxf.status > 0]
if viewports[0].dxf.get("status", 1) == 1:
    viewports.pop(0)          # ← 丢掉 status==1 的视口
```

上方原注释：`status==1` = *the active viewport*，它
*determines how the paperspace layout is presented as a whole*。
**但 `1` 恰恰是磁盘上 AutoCAD 标准文件的正常值。**

**处理**：只在**内存副本**把 `status` 抬到 `2`；渲染完不回写不保存，
磁盘 DXF 保持 `status=1`，AutoCAD 打开行为不变。

### 19.3 识图读取通用化（读外部真实图纸）

| 问题 | 现象 | 处理 |
|---|---|---|
| 中文名写成 `\U+XXXX` | 图层名变 `IRC\U+5929\U+82B1`，按图层名匹配的规则**静默全不中** | `_decode_uesc()` 解码 |
| 无名图层 / 缺名表记录 | ezdxf 加载阶段抛 `DXFTypeError`，**读不了** | 跳过无名图层；失败自动退 `recover` |
| 畸形 OBJECTS 段 | 严格与 recover 都报错 | `examples/dxf_prepare.py` 剥段 |

配套：`examples/dwg2dxf_convert.py`（DWG→DXF，**必须 `CadOutputMode.CONVERT`**）、
`examples/dxf_prepare.py`（**按 (code,value) 成对解析** —— DXF 的"值"行本身可以是 `0`）。

### 19.4 ② 识图通用化实测结论

样本：7 个真实 DWG（材料表/通用剖面图/1F/-1F/2F/3F/冷暖施工图），人工标注真值。

**能做的**：DWG→DXF 平均 **8.6s/文件**、膨胀 7.8 倍 → 182 文件约 **26 分钟 / 3.9GB**；
预处理+容错加载后 **7/7 可读**（修复前 2/7 崩或残）。

**不能做的**：现成 `infer_drawing_type` 打真实图纸 ——
严格 **0/7**、宽松 **1/7**、**认错 6/7（86%）**。

> ⚠️ 度量教训：旧"识别成功 100%"是**假指标**（只问有没有输出，不问对不对）。
> 本样本它给 100%，真实严格正确率 0%。**识图评测必须报认错率。**

**三条根因**：① 规则只认自家 `G_WALL` 图层命名，真实图纸是 `A建墙`/`IRC灯具` → 零命中；
② 模板图例污染（189 行出现在全部 7 文件，且**图名"首层平面图"本身是模板行**，去模板会删掉答案）；
③ 图层是**文档级**（6 张图共享同一套 120~469 图层）→ 只能判"图集"判不出子类型。

**方向**：信号在**版式/几何**（材料表 msp 仅 262 实体 vs 平面 29361），
但必须先建 **30~50 张评测集**（`benchmarks/`），否则阈值一定过拟合。

**边界**：✅ 可承诺"读得进"外部 DWG/DXF；❌ 不可承诺"认得出"图别（0~14%，86% 自信认错）。

### 19.5 验证

`verify_layout_notes` 69 + `verify_v114` 60 + `verify_pipeline_v115` 57 +
`verify_nl` 46 + `verify_v115` 37 + `verify_interfaces` 21 + `verify_pipeline` 4
= **294 项全绿**（v1.17.1 改动后复跑，零回归）。

### 19.6 待办

| # | 事项 | 状态 |
|---|---|---|
| ① | **16G101 标准图册（templates_atlas 包）** | ✅ **v1.17.2 已交付**：`rebar_calc.py`（钢筋计算，26/26 核对）+ `pingfa.py`（平法标注生成器，53/53）+ 三层架构/Q1-Q3 设计已拍板 |
| 1 | **续页图幅命名模板化** | ✅ **v1.17.3 已交付**：`gb_standards.py` 新增 `NOTE_SHEET_NAME_TEMPLATE="{base}_notes_{n}"` + `get_note_sheet_name()` + `BorderStandard.note_sheet_name()`；NL 引擎改调它、支持 `template=` 覆盖与中文后缀回退；`verify_continuation.py` 7/7 |
| 2 | 建识图评测集 | ⚠️ **v1.17.6 更正**：v1.17.4 脚手架 + v1.17.6 扩到 21 张，但**13 张"真实图纸"实为 `ezdxf` 自产**（样本污染），已引入 `provenance` + `check_provenance.py` 闸门 + 分组报数。**真实样本仍只 8 张**。<br>✅ **v1.17.8 已入库 18 张**（14 单图别 + 4 OOV），`ground_truth.json` v1.3→**v1.4**，闸门 exit 0，真实基线已出：**external n=26 · 严格正确 3 (11.5%) · ★认错 19 (73.1%)**。<br>❗ **仍未完成**：离 30 张目标差 4 张 |
| 2b | **给 `read()` 加弃权能力** | ✅ **v1.17.9 已交付**：`DrawingInfo` 新增 `type_margin`（裕度 = top1-top2）/ `abstained` / `abstain_reason`；新增 `score_drawing_types()` / `infer_drawing_type_ex()` / `set_abstain_thresholds()` / `PLACEHOLDER_TYPE`；门槛不达标或**并列**时弃权（图别回退占位符，**不写 None** —— 保下游字符串契约）。★ 实证：**conf 阈值是空操作**（只有 {0.0,0.5,0.7} 三档、对错完全重叠，0.3/0.4/0.5 与关闭逐桶相同），**margin 才有判别力**（margin≥0.2 → 自信答错 16→3、严格正确零损失）。门槛**默认关闭**保基线可复现。<br>⚠️ **代价**：弃权只把「危险错误」搬进「空答」桶，`★认错` 总数不变（19）→ 必须单列**危险率**当第一指标 |
| 2c | **查 `火灾报警图` 误判簇** | ❗**v1.17.8 新增 · 压认错率第一顺位靶子**：26 张里 **5 张**被齐刷刷识成 `火灾报警图`（`01 材料表`/`02 通用剖面图`/`03 1F`/`05 2F`/`06 3F`）= 16 处自信答错的 **31%**，疑为**模板图例污染**。**优先于调关键词** |
| 2d | 统一 `raw/` 的预处理状态 | v1.17.8 发现：基线 13 张（桃花源）**仍带 OBJECTS 段**（未过 `dxf_prepare`），别墅 18 张已过。因 `drawing_reader.read()` 不调 `prepare`，raw/ 里放什么就读什么。不影响识别口径，但**严格可比需统一** —— 会改历史基线，须单独评估 |
| 3 | 版式信号识别 | 基于实体数/几何统计做图别判别，替换"关键词袋"路线（**前置：样本 ≥30 张**，否则必过拟合）|
| 4 | 节点大样 node_*.py | ✅ **v1.17.3 已交付**：③ 层四文件 `node_beam_column`/`node_stair`/`node_foundation`/`node_steel`，复用 bar/hook 独立函数，NODES 注册表，`verify_nodes.py` 29/29 |
| 5 | 节点大样接入 NL 路由 | ✅ **v1.17.4 已交付**：四类节点关键词进 `NLPParser.DRAWING_TYPE_PATTERNS`，`_dispatch` 增 4 handler，「600x600柱300x600梁的中柱节点」直接出图；`verify_node_routing.py` 23/23 |
| 6 | 统一验证入口 | ✅ **v1.17.4 已交付**：`examples/verify_all.py`（18 套件一键跑 + 汇总 + `--list/--json/子集过滤`）；顺带修掉「探测类 ❌ 被当失败」的误判；`verify_steel_nodes.py` 24/24 |
| 7 | 钢结构节点扩展 | ✅ **v1.17.4 已交付**：`node_steel_v2.py`（钢柱脚/钢梁柱栓焊混接/钢梁拼接）+ 独立注册表 `STEEL_NODES` + NL 路由 3 类图别 |

## 二十、标准图册 templates_atlas 包（v1.17.2 新增 · 扩「能画什么」）

包位置 `scripts/templates_atlas/`（`__init__.py` / `rebar_calc.py` / `pingfa.py`）。地基 `rebar_calc.py` 按 GB 50010-2010 / 16G101-1 算锚固/搭接/弯钩/保护层/箍筋；上层 `pingfa.py` 平法标注生成器。

### 20.1 三层架构（与 templates_struct.py 分工 · 已拍板）

| 层 | 模块 | 画什么 | 标准 |
|---|---|---|---|
| ① 通用构件配筋 | `templates_struct.py`（既有） | 单构件钢筋一根根画（柱/梁/板/基础/楼梯） | GB/T 50105-2010 |
| ② 平法注写 | `templates_atlas/pingfa.py` | 国标注写符号标在平面，不画钢筋 | 16G101-1/2/3 |
| ③ 标准节点大样 | `templates_atlas/node_*.py`（**v1.17.3 已交付**） | 16G101 连接构造（梁柱节点/楼梯/基础插筋/桩基），**真画钢筋排布** | 16G101-1/2/3 |

### 20.2 六类平法节点（② 层）

| 节点 | 函数 | 国标 | 关键注写 |
|---|---|---|---|
| 柱 KZ | `column_pingfa_note(ColumnPingfaParams)` | 16G101-1 柱表 | `b×h b1 b2 h1 h2` / 角筋 / b边·h边中部筋 / 箍筋类型号 `1(4×4)` / 箍筋 |
| 梁 KL | `beam_pingfa_note(BeamPingfaParams)` | 16G101-1 平面注写 | `KL2(2A) 300×700` / `Φ8@100/200(2)` / `2Φ25` / `G4Φ12` / `(-0.100)` + 原位 `6Φ25 4/2` |
| 板 LB | `slab_pingfa_note(SlabPingfaParams)` | 16G101-1 板块集中标注 | `LB1 h=120` / `B:X&Y Φ10@200` / `T:X&Y Φ10@200` |
| 剪力墙 Q | `wall_pingfa_note(WallPingfaParams)` | 16G101-1 墙身 | `Q1 200` / 水平·竖向分布筋 / 拉筋 |
| 楼梯 AT | `stair_pingfa_note(StairPingfaParams)` | 16G101-2 | `AT1 h=120` / `1800/12=150` / 下·上·分布筋 |
| 独立基础 DJ | `found_pingfa_note(FoundPingfaParams)` | 16G101-3 | `DJj1 2400×2400` / `B:X&Y Φ14@200` / 基础底标高 |

梁注写 `show_anchor=True` 可附 `rebar_calc` 实算 `laE=` 抗震锚固长度。

### 20.3 钢筋计算器 rebar_calc.py（①/③ 层地基）

`anchorage_length(d,grade,concrete,seismic,level)`→`lab/la/laE`（公式 `lab=α·(fy/ft)·d`，α=0.14 带肋）；`lap_length`→`ll/llE`（ζl: ≤25%→1.2/50%→1.4/100%→1.6）；`hook_length`（90°=12d / 135°箍筋=max(10d,75)+1.9d / 180°=3d+6.25d）；`cover_thickness`（16G101-1 表 8.2.1）；`stirrup_length`（内皮周长+两钩）。`examples/verify_rebar_calc.py` **26/26** 核对合 GB 50010-2010 / 16G101-1。

### 20.4 节点库设计结论（Q1-Q3）

- **Q1**：非「节点大样 vs 配筋图」二分，而是三层架构（见 20.1）；`templates_struct`=通用构件配筋，`pingfa`=平法注写，`node_*.py`=标准连接节点大样，互补。
- **Q2**：③ 层直接复用 `templates_struct` 的 `bar()/bar_label()/hook()/bar_mark()` 作基元；节点=bar 排布+hook 锚固+bar_label 标注+复合箍，缺啥补啥，不预建。
- **Q3**：③ 层 = builder-agnostic 独立函数 `func(b,params,x,y)`；不绑 GBDxfBuilder（国标图框是输出承载层、正交）。
  ~~不绑 NL 路由（节点精确，显式调用）~~ —— **该结论已被 v1.17.4 覆盖**：③ 层仍保持"显式调用可用"，
  同时**新增** NL 路由入口（`node_*` → handler），两种用法共存；注册表 `NODES` 供 pipeline/NL 发现（v1.17.4 已用上）。

### 20.5 实测

- `examples/pingfa_demo.py` → `out_pingfa/pingfa_综合标注.dxf`(98 实体)+`pingfa_全节点.dxf`(98 实体)。
- `examples/verify_pingfa.py` → **53/53**；`examples/verify_rebar_calc.py` → **26/26**。
- 未改 `dxfkit.py` 与既有模板，**294 项历史验证零回归**。`scripts/templates_pingfa.py` 已改为 `templates_atlas.pingfa` 再导出薄层（单一真相源）。

### 20.6 ③ 层节点大样（v1.17.3 新增 · 真画钢筋）

四个独立函数（builder-agnostic），复用 `templates_struct.bar()/hook()/bar_label()/bar_mark()` 基元，锚固长度实算自 `rebar_calc.anchorage_length`：

| 节点 | 函数 | 国标 | 画什么 | 验证实体数 |
|---|---|---|---|---|
| 框架梁柱节点 | `node_beam_column.node_beam_column(BeamColumnNodeParams)` | 16G101-1 | 柱纵筋(四角+四周)+核心区箍筋+梁上下纵筋(端部 15d 弯锚)+laE 标注；`node_type` 支持 中柱/边柱/角柱/顶层端/顶层中柱 | 中柱55/边柱53/角柱41（自测） |
| AT 梯板支承 | `stair_node(StairNodeParams)` | 16G101-2 | 斜梯板+平台梁+下部筋贯通(两端锚入梁)+分布筋+laE | 56 |
| 柱基础插筋 | `foundation_node(FoundationNodeParams)` | 16G101-3 | 独立基础+柱插筋(底部 15d 弯折 a)+laE | 60 |
| 桩基承台锚固 | `pile_node(PileNodeParams)` | 16G101-3 | 承台+桩(圆)+桩顶伸入承台(灌注50/预制100)+锚固筋 laE | 45 |

> **梁柱节点重规格说明（v1.17.3 桌面规格重写）**：`BeamColumnNodeParams` 用
> `column_width`/`column_depth`（柱宽/柱深）+ `beam_left_*`/`beam_right_*`/`beam_top_*`/`beam_bottom_*`
> （左/右/上/下四向梁宽高），配快捷函数 `middle_node/edge_node/corner_node` 与 `NODE_TEMPLATES`
> 字典；`validate_beam_column` 抓柱纵筋非 4 倍数 / 抗震等级越界(0~4) / 节点类型未知。
> 桌面规格误用的 3 个 builder 名（`add_hatch_pattern`/`add_line`/`add_dimension`）已按 dxfkit 真实 API
> 改写（`add_hatch(pts,pattern,scale,angle,layer)`/`line`/`dim_h`/`dim_v`）。

`templates_atlas.NODES` 注册表聚合四节点：`{key:(draw, Params, validate, doc)}`（`beam_column`/`stair`/`foundation`/`pile`），供 pipeline/NL 后续发现。
`examples/node_demo.py` → `out_nodes/nodes_综合大样.dxf`（四节点同图，216 实体）。
`examples/verify_nodes.py` → **29/29**（四节点生成+Φ/laE 落盘+validate 正负样本+NODES 完整+注册表驱动）；
`examples/verify_node_beam_column.py` → 节点类型/参数/laE 三组全过。

**总验证**：294（历史）+53(pingfa)+26(rebar_calc)+29(nodes)+7(continuation) = **402+ 项零 FAIL**。

### 20.7 续页命名模板化（v1.17.3 · ① 收尾）

把说明续页图幅的命名从硬编码中文后缀 `'GB_%s_说明%d'` 抽成可配置模板，便于非中文环境 / 自定义图号体系。

**新增 API（`scripts/gb_standards.py`）**：

| API | 作用 |
|---|---|
| `NOTE_SHEET_NAME_TEMPLATE = "{base}_notes_{n}"` | 模块级默认模板；默认 → `GB_A3_notes_2` |
| `get_note_sheet_name(base, paper, n, title="", template=None)` | 模块函数；`template` 缺省用默认模板 |
| `BorderStandard.note_sheet_name(cls, base, paper, n, title="", template=None)` | 类方法封装，等价调用 `get_note_sheet_name` |

**NL 引擎改动**（`natural_language_engine._draw_notes_pages`）：原 `'GB_%s_说明%d' % (paper, idx+1)` →
```python
name=BorderStandard.note_sheet_name(
    base="GB_%s" % paper_size, paper=paper_size,
    n=idx + 1, title=td.get('title', '')),
```

**向后兼容**：传 `template="GB_{paper}_说明{n}"` 即回退旧中文命名 `GB_A3_说明2`；`name=` 透传仍生效（命名可完全自定义）。
`examples/verify_continuation.py` → **7/7**（默认命名/中文回退/name 透传/自定义模板/类方法≡函数/NL 不再硬编码/NL 已改用 `note_sheet_name`）。

### 20.8 ③ 层节点接入自然语言路由（v1.17.4 · 扩「怎么调用」）

v1.17.3 把四个节点**画出来**，v1.17.4 把它们的**调用入口扩到自然语言**——一句中文直出图。

**四类节点触发词**（已加入 `NLPParser.DRAWING_TYPE_PATTERNS`，排在弱匹配之前）：

| drawing_type | 触发词 | 默认尺寸(mm) |
|---|---|---|
| `node_beam_column` | 梁柱节点/框架梁柱节点/框架节点/梁柱连接/柱梁节点/中柱节点/边柱节点/角柱节点/顶层端节点/顶层中柱节点 | 3000×2000 |
| `node_stair` | 楼梯节点/梯板配筋/梯板支承/AT型楼梯/梯段节点 | 2000×4000 |
| `node_foundation` | 基础节点/独立基础/承台插筋/柱插筋/基础插筋/基础大样 | 3000×3000 |
| `node_pile` | 桩基节点/桩基锚固/桩承台/桩头锚固/桩基大样 | 2500×2500 |

**接入位置（4 处，全在 `natural_language_engine.py`）**：`NLPParser.DRAWING_TYPE_PATTERNS`（类属性）、
`DEFAULT_DIMS_MM`、`NaturalLanguageGenerator._dispatch`（实例字典）、`discipline_map`（→structural）。

**参数解析 `_parse_node_text`**：柱/梁截面支持两种语序（`600x600柱300x600梁` / `柱800×800 梁400×800`）；
规则为**零间隔相邻 + 尺寸在名词前优先**（避免"柱"被夹在两个尺寸对之间时误判）。
另解析 `node_type`（中柱/边柱/角柱/顶层端/顶层中柱）、`seismic_level`（一~四级）、`concrete_grade`（C30…）、`rebar_grade`（HRB400…）。

**本轮真实报错与修复**：

| 坑 | 修复 |
|---|---|
| `from templates_atlas.node_stair import node_stair` → ImportError | 真实函数名 `stair_node` / `foundation_node` / `pile_node`（`node_beam_column` 例外）|
| 无 `node_pile.py` | 桩基在 **`node_steel.py`**（`pile_node` + `PileNodeParams`）|
| 误以为 dispatch 在模块级 `NL_DISPATCH` | 实为 `self._dispatch` 实例字典 |
| 误以为关键词表是模块级变量 | 实为 `NLPParser` 类属性 |
| 「600x600柱300x600梁」柱被判 300x600 | 改零间隔相邻 + 前置优先 |

> **pipeline 无需改**：`SPECIALTY_TEMPLATES` 是空字典且不在生成路径上，pipeline 走 `generate_from_text`，
> 节点进 NL 引擎即自动覆盖。桌面规格的 `nl_router_patch.py`/`pipeline_patch.py` 属死代码，未采用。

`examples/verify_node_routing.py` → **23/23**（路由 5 + 参数 5 + 注册 8 + 既有图别 5）。

### 20.9 钢结构连接节点（v1.17.4 · D 路线）

补 `node_steel.py`（只有混凝土桩基承台）的缺口，新增**钢结构三类连接节点**
（`scripts/templates_atlas/node_steel_v2.py`），约定与 ③ 层一致（builder-agnostic 独立函数 + dataclass + `validate_*`）：

| 节点 | 函数 / 参数 | 依据 | 画什么 | 实体数 |
|---|---|---|---|---|
| 钢柱脚 | `steel_column_base(SteelColumnBaseParams)` | GB 50017-2017 / 16G519 | 底板 + 二次浇筑层 + 工字形柱身 + 加劲肋(刚接) + 锚栓(铰接4/刚接8) + 锚固段 | 27 |
| 钢梁柱节点 | `steel_beam_column(SteelBeamColumnParams)` | GB 50017-2017 / 16G519 | 工字形柱 + 工字形梁 + 端板 + 翼缘焊缝(全/部分熔透) + 腹板高强螺栓双列 | 29 |
| 钢梁拼接 | `steel_beam_splice(SteelBeamSpliceParams)` | GB 50017-2017 / 16G519 | 左右两段工字形梁 + 翼缘/腹板拼接板 + 高强螺栓 | 31 |

- 注册表 **`STEEL_NODES`**（3 项）**独立于 `NODES`**（`verify_nodes.py` 断言 `len(NODES)==4`，并入会破坏既有验证）。
- **NL 路由**：新增 `node_steel_base` / `node_steel_beam_column` / `node_steel_splice` 三类图别。
  ⚠️ **顺序敏感**：钢结构规则必须**先于混凝土节点**——「钢梁柱节点」含子串「梁柱节点」，
  否则会被 `node_beam_column` 抢走（实测踩过）。
- `examples/verify_steel_nodes.py` → **24/24**；`python scripts/templates_atlas/node_steel_v2.py` 直跑自测。

### 20.10 识图评测集 + 统一验证入口（v1.17.4 · B/C 路线）

**B · `benchmarks/`（识图评测集）**

| 文件 | 作用 |
|---|---|
| `ground_truth.json` | 真值表：`file / type / family(同族) / oov(是否在类型域) / **provenance(来源)** / source` |
| `eval.py` | 评测脚本，真实 API `drawing_reader.read(path).drawing_type`；**按来源分组报数** + `--external-only` |
| `check_provenance.py` | **来源取证闸门**（v1.17.6 新增）：`$LASTSAVEDBY` + 内嵌自述判定真伪，`--gate` 不符即 exit 1 |
| `import_samples.py` | 把外部 DXF 按逻辑名拷进 `raw/`（`raw/` 已 .gitignore，136MB 不入库） |
| `README.md` | 口径 / 基线 / 根因 / 边界 |

五口径：严格正确 / 宽松正确（**不含严格**，各桶互斥）/ 正确拒识（**OOV 真值下弃权=合格**）/ 空答 / **认错**。
> ⚠️ 度量纪律 #1：「识别成功 100%」是**假指标**（只数"有没有输出"，不数"对不对"），本项目实测可同时成立。
> ⚠️ 度量纪律 #2：`read()` 低置信返回占位符 `"未识别"`（非空）→ `refused` 桶**结构性恒为 0**，
> 且"如实说不知道"与"自信答错"同罪 → 现视同**弃权**并单列 `abstain`（认错 = 自信答错 + 空答）。
> ⚠️ 度量纪律 #3：**自产样本会稀释认错率** → **认错率只认 `external` 分组**。
> 内置自洽校验：`strict+loose+refused+abstain+errored+wrong == n`。

**★ 校正基线（8 张真实出图）**：严格 **0/8 (0%)**、宽松 1/8、**认错 7/8 (87.5%)**。
自产 13 张对照：严格 5/13 (38.5%)、认错 8/13 (61.5%)。
根因：① 规则只认自家图层命名（真实图纸用 `A建墙`/`IRC灯具` → 零命中）；
② 模板图例污染（189 行图例文字出现在全部 7 文件，图名"首层平面图"本身是模板行）；
③ 图层是"文档级"判不出图纸级；④ **系统从不弃权**（拒识率恒 0）。
**可判别信号在版式/几何**：外部真实图纸 **10,027~29,361 实体** vs 自产 **56~375 实体**。
**样本 <30 张调阈值必过拟合 → 先扩到 30~50 张再改规则。**
**扩样本 SOP**：`import_samples.py` → `check_provenance.py --apply` → `--gate` → 才允许 `eval.py`。

### 20.11 DWG→DXF 转换器的读不了问题（v1.17.6 / v1.17.7 / v1.17.8 —— 共五类，全批已 0 失败）

`examples/dxf_prepare.py` 新增 `repair_missing_seqend()` / `strip_icon_xdata()` /
`normalize_binary_chunks()` / `repair_missing_endsec()` / `drop_empty_handles()`
并接入 `prepare()`。

**第 ① 类：`BLOCKS` 段漏写 `SEQEND`**
- **根因**：aspose `CadOutputMode.CONVERT` 把 `POLYLINE+VERTEX*` / `INSERT+ATTRIB*`
  写进 **`BLOCKS` 段**时**漏写结尾的 `0 SEQEND`**（`ENTITIES` 段正常）。
  违例数与 BLOCKS 段 POLYLINE 数完全相等 → `ezdxf` 严格/recover **都读不了**。
- **代价记录**：初版用"只前进不写出"的 skip 写法，把实体自身组码吞掉
  （`平立剖面图1` 丢 119,188 行 = **35%**），而 **ezdxf 照常能读、不报错**。

**第 ② 类：`INSERT` 声明 `66=1` 却无 ATTRIB**（`repair_missing_seqend` 补2）
- `INSERT` 带 `66=1`（attribs_follow）却**一个 ATTRIB 都没写、也无 SEQEND** →
  `Expected DXF entity INSERT or SEQEND`（`ezdxf/entities/subentity.py:184`，
  消息里的类型是**实际撞到的**实体）。
- 机制已核实到源码级：`attribs_follow` 在 `Insert` 里是 **`DXFAttr(66)`**
  （`entities/insert.py:89`，位于 `acdb_block_reference` 子类），会被严格加载；
  `subentity.py:189` 据此进入链接子实体循环 → 撞见非 ATTRIB/SEQEND 实体即抛错。
- ⚠️ **不能靠"看到 ATTRIB 才补"** —— 它一个 ATTRIB 都没有，判据必须是 **`66=1` 声明本身**。
  24 个文件里**只有 `三维模型`** 有此缺陷（3 处），修后 **strict ✅**。
  **承重已实测**：把该判据回退成"只看 ATTRIB"，文件立刻复现
  `DXFStructureError: Expected DXF entity INSERT or SEQEND`。

**第 ③ 类：XDATA 二进制块写成"带横线十六进制"**（`strip_icon_xdata` / `normalize_binary_chunks`）
- 组码 `1004` 的值写成 `28-00-00-00-20-…`（AutoCAD `.pat`/打印格式），DXF 要求**纯十六进制**
  → `Invalid binary data near line: 5156`。实测 85 处**全在 APPID `CONTENTBLOCKICON`**（块缩略图图标）。
- 修：整块删图标 XDATA + 残留横线块去横线归一（原地替换，不进守恒账）。

**第 ④ 类（v1.17.8）：`TABLES` 段失去 `ENDSEC` —— ⚠️ 这一类是我们自己造的**
- **根因**：`_repair_table_records()` 原按"下一个 `NAMED_TABLES` 记录头"切记录边界。
  排在**表末尾**的那条无名记录，会把紧跟其后的 `0 ENDTAB` / `0 ENDSEC` 一起算进
  自己的记录体；该记录因无名被丢弃时，**段尾标记跟着陪葬** → TABLES 段永久失去 ENDSEC
  → `DXFStructureError("missing ENDSEC tag.")`（recover 能救，fix=2）。
- **触发条件**：TABLES 段里**最后一条表记录缺 `2` 名字组**（aspose 产物里真实存在）。
  实测 `别墅结构1.09` 命中；原文件还有 `DXFTypeError: name has to be a string, got NoneType`。
- **修法（双保险）**：
  1. **治本**：记录边界改为在 `ENDTAB`/`ENDSEC`/`TABLE` 处也切断
     （新增 `TABLE_RECORD_HEADS = NAMED_TABLES ∪ {ENDTAB, ENDSEC, TABLE}`）；
  2. **兜底**：`repair_missing_endsec()` 事后给未闭合的 SECTION 补 ENDSEC。
- 修后 `别墅结构1.09` **补ENDSEC=0**（边界修法已从源头保住），strict ✅。

**第 ⑤ 类（v1.17.8）：`ENDBLK` 空句柄**（`drop_empty_handles`）
- `ENDBLK` 的 `5`（句柄）是**空值** → 严格模式 `ValueError: Invalid handle .`；
  recover 只能 `skipped invalid handle ... in DXF entity "ENDBLK"` 跳过该实体。
  空句柄在 DXF 里**任何情况下都非法**（句柄必须是十六进制串）。
- 修：删掉该 `5` 组，ezdxf 会像处理"无句柄旧版 DXF"一样自动补新句柄。
  实测 `E门窗` 12 处、`E立面` **19 处** → 两张都从 recover 升到 **strict ✅**。
- ⚠️ 同类噪声但**无需处理**：`E门窗` 的 450~459（MLINE 组码）有 91 处横线十六进制，
  但**全部宿主在 OBJECTS 段的 XRECORD**，随 ① 的整段删除一并消失（clean 实测 0 处）。

**效果**：别墅 24 张 **严格可读 24 / 需 recover 0 / 失败 0**
（v1.17.7 时是 21/3/0，v1.17.6 前是 22/0/2 彻底读不了）。

**守恒断言**（扩容后）：
`输出组数 == 输入组数 - 删OBJECTS组 - 删畸形表记录组 - 删图标XDATA组 + 补SEQEND组 + 补ENDSEC组 - 删空句柄组`
，不守恒即抛错拒写。实测 **24/24 ✅**。
**另加两道结构自检**（v1.17.8，防"能写出但读不了"）：每个 SECTION 必须有 ENDSEC；
不得残留空句柄组。任一不满足 → `AssertionError` 拒写。

**★ 新增 `examples/verify_prepare.py`（v1.17.8）—— 补上最大的一块验证空洞**
> 五类问题累积到 v1.17.7 都**没有任何套件覆盖 `dxf_prepare`**，所以第 ④ 类
> （自己吞掉 ENDSEC）能一直潜伏到别墅图纸集才被撞出来。
> 该套件用**合成 DXF**（ezdxf 生成基线 + 文本层注入缺陷），**不依赖任何外部图纸数据集**，
> 每类都要求：① 注入后确实读不了（负向对照）→ ② prepare 不抛异常且守恒 →
> ③ prepare 后严格模式可读 → ④ 关键结构仍在。**53 断言全绿**。
> ⚠️ 两个注入陷阱（已写进脚本注释）：⑤ 的基线里 INSERT 后**必须再跟一个实体**
> （若后面直接是 ENDSEC，链接循环自然结束、不会报错）；⑦ 必须**删掉 `2` 组**
> 而不是置空串（空串仍是 `str`，不报错；只有组缺失才让 `name=None`）。

### 20.11b 来源闸门新增"结构指纹兜底"（`benchmarks/check_provenance.py`）

- **问题**：aspose CONVERT **无条件抹掉 `$LASTSAVEDBY`**（别墅 24 张实测 **24/24 全空**）
  → 旧规则判 `unknown` 会把**整批真实图纸卡死**在闸门外。
- **解法**：新规则 4 —— `$ACADVER ∉ {AC1024,AC1027,AC1032}` **且**
  （`实体 ≥ 1000` **或** `图层 ≥ 20`）→ `external`。
  | 指标 | self_generated（13 张） | external | 分界 |
  |---|---|---|---|
  | `$ACADVER` | 100% **AC1024** | AC1015/AC1018 | — |
  | 实体数 | 6 ~ 375 | 1,221 ~ 156,649 | 1000 卡在 375↔1221 |
  | 图层数 | 8 ~ 17 | 2 ~ 469 | 20 卡在 17↔20 以上 |
  并联是为兜住"实体少但图层多"的真实图（修好的 `三维模型` 277 实体/35 图层）。
- **⚠️ 已知局限（故意方向）**：真实图也有实体少+图层少的（`01 材料表.dxf` 仅 262 实体）。
  这类若又被抹元数据 → 判 `unknown` 卡闸门。**宁可卡闸门，也不误收脚本产物**。
- ✅ 对现有 21 张**零影响**：`external=8 / self_generated=13 / unknown=0`，`--gate` exit 0。

### 20.12 外部素材勘察：《百套别墅建筑设计CAD图纸》（2026-09-12 · 含 A 方案落地）

- 目录 `F:\BaiduNetdiskDownload\AUTOCAD图纸 百套别墅建筑设计CAD图纸免费下载\30070062\别墅建筑`：
  **188 个 DWG / 472 MB**，全部 AC1014/AC1015（R14/2000），专业**只有别墅建筑**（无工业/市政/机电）。
- 命名可标注性（纯看文件名）：能定**单一**图别 **24** / 含 ≥2 图别 **5** / 表明"整套·方案" **36** /
  3D·效果图 **3** / 无图别信息 **117**。
- ★ **但名字像单图别 ≠ 内容是单图别**：对 24 张 A 类做**内容取证**后
  （扫**模型空间 + 全部块定义 + INSERT 的 ATTRIB** + 解码 `\U+XXXX` 转义；
  只扫模型空间会**漏掉标题栏块里的图名/图号**，图别命中几乎全空），
  **只有 14 张**内容真属单一图别 → 另 **5 张**实为多图别套图（详见下表）。
- 一 DWG = 一整套图（多图别并排排在模型空间，布局只有 Model+Layout1）→ 与"一文件=一图别"框架不匹配。

**被剔除的 5 张（内容实为多图别套图）**

| 文件 | 证据 |
|---|---|
| `别墅建筑结构全套图纸` | 实体 **156,649**；强命中 平面22 + 立面12 + 配筋4 + 门窗表2 |
| `别墅结构` | 强命中 **12 种**图别；文字含『基础层梁钢筋图/3层板钢筋图』 |
| `钢结构别墅建筑` | 强命中 平面8 + 立面8 + 剖面4 → 建筑全套 |
| `E平面` | 强命中含 剖面14 + 立面8；图框关键词有『**图纸目录×2**』 |
| `平面` | 图框关键词 设计60/比例30/图号30 → **约 30 个图框**，是**图集** |

> 这 5 张是将来 B 方案（真值=图别集合）的现成第一批数据。

**⚠️ 三个方法学陷阱（已写进判据）**
1. **真值不可用文字关键词自动反推** —— 文字正是识别的输入特征，同源标注 = **循环论证**，
   会造出"识别率 100%"的假结果（污染事故的另一形态）。真值必须**人确认**，机器只给"建议+证据"。
2. **图签里的专业名不是内容** —— `2-270-节点大样` 的标题栏块（`tuA2|图签`）含
   『电气/给排水/建筑/结构/暖通』五个专业名，按关键词计数会误判为多图别。
3. **文件名可以说谎** —— `错层别墅建筑结构施工图.dwg` 内容实为**平面图**（平面图×33）。

**A 方案结论（v1.17.8 已落地）**：转出 24 张候选 → **入库 18 张** =
14 张单图别（平面图3 / 立面图3 / 节点大样图4 / 结构配筋图3 / 结构图1）+ 4 张 OOV
（透视图/鸟瞰图/三维模型 + `E门窗`，词表无"门窗图"类）。
external 真实样本 **8 → 26 张**（离 30 张目标仍差 4 张）。
交付：`outputs/别墅样本_入库与真实基线报告.md`（人读）+ `benchmarks/ground_truth.json` v1.4（机器读）。

**★ v1.17.8 真实基线（`eval.py --external-only`，external n=26）**

| 口径 | v1.17.6 (n=8) | v1.17.8 (n=26) |
|---|---|---|
| 严格正确 | 0 (0%) | **3 (11.5%)** |
| 宽松正确 | 1 | 1 (3.8%) |
| 正确拒识 | 0（桶**结构性恒为 0**） | **3 (11.5%)** ← 首次出现 |
| **★ 认错** | **7 (87.5%)** | **19 (73.1%)** |
| 　自信答错 | 7 | 16 (61.5%) |
| 　空答/弃权 | 0 | 3 (11.5%) |

⚠️ **87.5% → 73.1% 不是能力提升**，是分母从 8 张换成 26 张。**禁止当作改进证据。**
- **首次出现「正确拒识」**：3 张 3D/效果图**真的弃权了** → 待办 #2b「给 `read()` 加弃权能力」
  这条路**已被证明可行**。
- **最大错误簇：5 张被齐刷刷识成 `火灾报警图`**（`01 材料表`/`02 通用剖面图`/`03 1F`/`05 2F`/`06 3F`），
  占 16 处自信答错的 **31%**，疑为**模板图例污染** → 压认错率的**第一顺位靶子**。

**`eval.py` 修 bug（度量教训 #4）**：OOV 样本真值是 `"type": null`，
`s.get("type","")` 取到 **`None`** → 打印 `truth[:10]` 抛 `TypeError`，**整批评测中断**。
修：统一 `(s.get("type") or "").strip()`。
⚠️ 教训推广：**`dict.get(k, default)` 只兜"键缺失"，不兜"值为 null"**，读外部 JSON 一律 `x.get(k) or default`。

**C · `examples/verify_all.py`（统一验证入口）**

```bash
python examples/verify_all.py            # 全部
python examples/verify_all.py nl nodes   # 子集
python examples/verify_all.py --json      # CI
```

判定顺序：① 显式计数 → ② 明确失败语 → ③ **明确通过语优先于标记计数** → ④ 兜底数 ✅/❌。
> ⚠️ 第 ③ 条是踩坑后加的：最初只「数 ❌」，`verify_fonts` 因列 6 个**本机未装字体候选**打 ❌
> 被误判为 6 项失败（脚本自身输出实为「✅ 验证通过」）——**探测类 ❌ ≠ 失败**。
> 自动发现新增 `verify_*.py`（排除自身防递归），rc 也参与判定。

**最终总验证（v1.17.4）**：**18 套件全绿 · 469 项可计数断言 · 0 失败**（约 1 分 32 秒）。

---

## 二十、v1.17.5 收口（E5 规范库 + E4 命名 + 说明栏可读下限修复）

### §20.1 E5 · 16G101 接入规范引用库（`construction_codes.py`）
- **根因**：旧 `get_codes_by_drawing_type` 只匹配 `'all' in applicability or drawing_type in applicability`，具体名图别（`node_beam_column`/`node_pile`/`node_steel_base`…）命中不了语义标签（`structural`/`beam`/`column`/`foundation`），节点图连 GB 50010/GB 50204/16G101 都漏引。
- 新增 `PINGFA_CODES`（16G101-1/2/3 平法图集，priority=1 强条）→ 并入 `ALL_CODES`，规范库 **64×13 → 67×14**。
- 新增 `DRAWING_TYPE_ALIASES` 把具体名图别归一为语义标签，匹配逻辑 = 精确名 / `'all'` / 别名 三者任一。
- `examples/verify_e5.py` **21/21**：节点图「执行规范」自动含 16G101。

### §20.2 E4 · `node_steel.py` 重命名为 `node_pile.py`
- 该文件装的是**混凝土桩基**节点（函数 `pile_node`/`PileNodeParams`），原名 `node_steel` 与钢结构混名。
- `mv` 改名（非 git 跟踪），同步 4 处引用：`templates_atlas/__init__.py`、`natural_language_engine.py`、`examples/node_demo.py`、`examples/verify_nodes.py`。
- ⚠️ 钢结构节点仍在 `node_steel_v2.py`（`STEEL_NODES`），**未受影响**。

### §20.3 说明栏「执行规范」可读下限回归修复
- 现象：E5 别名修复后 `floor_plan` 匹配规范 3→20+ 条，`_add_notes_to_sheet` 把规范块预留高度封顶 42%（真实 ~105mm），逼 `auto_fit` 把字高压到 2.2mm（< 2.5mm 可读下限）。
- 修复：① 预留高度封顶 0.42→0.6（让 `code_h` 贴近真实块高）；② `add_code_references` 的 `min_base_height` 3.0→**5.0**（最小字高 = base×0.5 = 2.5mm 恰为可读下限）。
- `examples/verify_layout_notes.py` **70/70**（原 69/70）。

### 最终总验证（v1.17.5）
**19 套件全绿 · 469 项可计数断言 · 0 失败**（约 2 分 50 秒）：
续页命名 7 · e5 — · 字体 — · 全量扩展 — · 接口层 21 · 布局+说明栏 70 · 自然语言 46 ·
梁柱节点 12 · 节点路由 23 · ③层节点 29 · 说明全套 — · 平法 53 · 端到端渲染 4 ·
NL 流水线 57 · 钢筋计算 26 · 钢结构节点 24 · 识图/算量/说明 60 · 3D 体量 37 ·
+ 高级模板+审图（无计数，按判定语通过）。

---

## 二十一、v1.17.8 收口（别墅样本入库 + 又两类读不了 + 补验证空洞）

### §21.1 又修两类「彻底读不了」
见 §20.11 的第 ④（`TABLES` 段失去 `ENDSEC`，**我们自己造的**）/ 第 ⑤ 类（`ENDBLK` 空句柄）。
别墅 24 张 **严格可读 24 / 需 recover 0 / 失败 0**。

### §21.2 补上 `dxf_prepare` 的验证空洞
`examples/verify_prepare.py`（**53 断言**）—— 合成 DXF + 文本层注入缺陷，**不依赖外部数据集**。
这是 v1.17.8 最关键的工程修正：`dxf_prepare` 累积 5 类缺陷却**零测试覆盖**，才让第 ④ 类潜伏至今。

### §21.3 `eval.py` 度量教训 #4
OOV 样本真值 `"type": null` → `s.get("type","")` 取到 **`None`** → `truth[:10]` 抛
`TypeError`，**整批评测中断**。修：`(s.get("type") or "").strip()`。
⚠️ **`dict.get(k, default)` 只兜"键缺失"，不兜"值为 null"** —— 读外部 JSON 一律 `x.get(k) or default`。

### §21.4 真实基线（external n = 26）
严格正确 3 (11.5%) · 宽松 1 · 正确拒识 3 · **★认错 19 (73.1%)**（自信答错 16 + 空答 3），桶自洽 ✅。
⚠️ 对比 v1.17.6 的 n=8 → 87.5%：**分母不同，不是能力提升**。
两个信号：① **首次出现「正确拒识」**（3 张 3D/效果图真的弃权了）；
② **最大错误簇 = 5 张误判成 `火灾报警图`**（占自信答错 31%），疑为**模板图例污染**。

### 最终总验证（v1.17.8）
**20 套件全绿 · 522 项可计数断言 · 0 失败**（约 1 分 21 秒）：
高级模板+审图 — · 续页命名 7 · e5 — · 字体配置 — · 全量扩展 — · 接口层 21 · 布局+说明栏 70 ·
自然语言解析 46 · 梁柱节点 12 · 节点 NL 路由 23 · ③层节点大样 29 · 说明全套 — · 平法标注 53 ·
端到端渲染(v1.12) 4 · NL 流水线 57 · **DXF 预处理 53（新增）** · 钢筋计算 26 · 钢结构节点 24 ·
识图/算量/说明 60 · 3D 体量 37。

---

## 二十二、v1.17.9 收口（识图「弃权」能力 + 阈值实证）

### §22.1 新字段（`DrawingInfo`）
| 字段 | 含义 | 默认 |
|---|---|---|
| `type_margin: float` | **决策裕度 = top1 - top2**（"赢得多干净"），**总是**计算 | `0.0` |
| `abstained: bool` | 是否拒绝作答（True 时图别为占位符） | `False` |
| `abstain_reason: str` | 弃权原因（人读） | `""` |

**★ 弃权时 `drawing_type` 仍写占位符 `"未识别"`，不写 `None`** ——
`pipeline.py` / `construction_notes_v2.py` / 各 demo / `to_dict()` / `to_markdown()`
**全部按字符串处理**，改 `None` 会把"拿不到答案"变成"崩掉"；弃权语义交给布尔字段。

### §22.2 新增 API（向后兼容）
`score_drawing_types()`（返回**全候选**得分，原函数只看 top1）·
`infer_drawing_type_ex()`（四元组；旧 `infer_drawing_type` 三元组**不变**，内部转调）·
`set_abstain_thresholds()`（运行时改门槛，供扫阈值）·
常量 `PLACEHOLDER_TYPE` / `ABSTAIN_CONF_THRESHOLD` / `ABSTAIN_MARGIN_THRESHOLD`。

### §22.3 ★★ 阈值实证（`eval.py --external-only --sweep`，n=26）
| 阈值 (conf, margin) | 严格正确 | 宽松 | 拒识 | 空答 | 自信答错 | ★认错 | 危险率 |
|---|---|---|---|---|---|---|---|
| **(0.0, 0.0) 现状** | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.3, 0.0) ← 直觉方案 | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.5, 0.0) | 3 | 1 | 3 | 3 | **16** | 19 (73.1%) | 61.5% |
| (0.6, 0.0) | **1** | 0 | 4 | 16 | 5 | **21 (80.8%)** | 19.2% |
| **(0.0, 0.2)** ← 实测最优 | **3** | 0 | 4 | 16 | **3** | 19 (73.1%) | **11.5%** |
| (0.0, 0.3) | **2** | 0 | 5 | 19 | **0** | 19 (73.1%) | 0.0% |
| (0.7, 0.2) | 1 | 0 | 4 | 18 | 3 | **21 (80.8%)** | 11.5% |

**三条硬结论**：
1. **置信度阈值是空操作**：`conf` 只有 `{0.0, 0.5, 0.7}` 三档，3 张严格正确的 conf
   (0.5/0.5/0.7) 与 16 张自信答错的 conf 完全重叠；门槛 0.3/0.4/0.5 与"关闭"**逐桶完全相同**，
   提到 0.6 **反而更差**（严格 3→1、未答对 19→21）。
2. **`margin` 才分得开**：`margin ≥ 0.2` → 自信答错 **16→3（-81.3%）**、严格正确 **3→3（零损失）**。
   因为 26 张里 **20 张 margin==0（并列），其中 16 张（80%）是自信答错** ——
   **"赢得很勉强"比"分数低"更值得警惕**。
3. **弃权不降低 `★认错`**：`fail = 自信答错 + 空答 + 读取异常`，弃权只是把「危险错误」
   **搬进**「空答」，总数恒为 19 → **必须单列「危险率」当第一指标**。

⚠️ n=26、严格正确仅 3 张，**任何阈值都是过拟合** → 门槛**默认 0.0（关闭）**，
并写断言锁死默认值。**翻转默认值的前置条件：external 扩到 ≥30 张。**

### §22.4 `eval.py`
- 优先读 `info.abstained`，**保留占位符兜底**（老 reader 无该字段时 `getattr(...,False)=False`，
  两条路径等价，有断言锁定）。
- 新增 `--abstain-conf` / `--abstain-margin` / `--sweep`。`--sweep` 先读一遍缓存 `(conf, margin)`
  再纯模拟（弃权是 `f(conf,margin)` 纯函数，不必每阈值重读 26 张图 —— 每遍 2~4 分钟）。
  **模拟保真度已验证**：`--abstain-margin 0.2` 实跑与模拟**逐桶一致**。
- 修契约瑕疵：OOV 样本 `truth==""` → `"loose": ""`（假值但是字符串），六个桶统一套 `bool()`。

### §22.5 ★ 新增 `examples/verify_abstain.py`（53 断言 · 合成 DXF）
防三类回归：① **行为回归**（默认 0.0 时识别结果与改动前**逐字一致**）；
② **泄漏回归**（造名为 `平面图.dxf` 但图内零平面图信号的图，**必须弃权** ——
用行为证明文件名没被当信号；不靠读源码断言）；③ **契约回归**（三元组/默认值/
`to_dict`/`to_markdown` + 不变式「`abstained` ⇔ 占位符」）。

⚠️ 泄漏背景：评测集 26 张里 **6 张文件名直接含真值关键词**，一传 filename
严格正确就从 3/26 虚增到 **9/26** —— **那是把答案喂给模型**。

### 最终总验证（v1.17.9）
**21 套件全绿 · 575 项可计数断言 · 0 失败**：
v1.17.8 的 20 套件全部保持（522 项）+ **识图弃权能力 53（新增）**。
**基线幂等性实证**：改动后 `eval.py --external-only` 与 v1.17.8 基线
**逐桶逐字完全相同**（3 / 1 / 3 / 3 / 16 / 19）。

---

## 二十三、v1.17.10 收口（清单报价「数据贯通」：bom 几何量 → 定额量 → 预算）

### §23.1 背景：边界 1 是最硬的缺陷
四项交付能力（材料清单统计 / 施工说明 / 工程量清单 / 清单报价）原本都能跑，但
**报价与图纸脱节**——`budget.py` 用 `建筑面积 × QUANTITY_PER_SQM × UNIT_PRICES`
（如混凝土 = 面积×0.38），与图纸实际画了什么**毫无关系**；而 `bom.py` 已经能测出
真实几何量（墙长 / 柱梁板基础 / 门窗）。中间缺的只是一层**换算**：
**几何量 → 定额量 → 喂给 budget**。本版本把这一层补上。

### §23.2 新增模块 `scripts/bom_to_budget.py`
| 组成 | 作用 |
|---|---|
| `ConvertParams` | 结构参数（墙厚 0.20 / 层高 3.0 / 柱 0.40 / 梁 0.25×0.50 / 板 0.12 / 基础深 0.60 / 含钢量 0.12 / 损耗 1.02 / 窗墙比 0.25 …），**全部可配** |
| `geometry_to_materials(report, p, unit_prices)` | 把 BOM 分类→定额量：墙体(m)→砌块(q×厚×层高×层×损耗)+内墙涂料(q×层高×2×层×(1−窗墙比))；柱(个/m)→混凝土体积+模板；梁(m)→体积+模板；板(m²)→体积+模板+地砖；基础→体积+模板；门窗→面积（均几何驱动）；累计混凝土→商品混凝土 C30、模板→木模板、混凝土×含钢量→钢筋 HRB400 |
| `estimate_from_bom(report, area, floors, p, unit_prices)` | ① 几何推导 ② **回退项**（地砖/防水/电线/PVC/水泥/砂，图纸未画结构层时按 `面积×QUANTITY_PER_SQM`，cov 标「回退(面积)」）③ 组装 `Material` 列表喂 `BudgetGenerator`；返回 `(budget, cov, acc, bg)` |

返回三件套：`budget`（总造价/单方）、`cov`（每项数据来源：几何(墙长)/几何(柱)/回退(面积)…）、
`acc`（各项工程量数值）。`cov` 即**数据溯源报告**，诚实标注每笔量从哪来。

### §23.3 接入标准流水线（opt-in）
`PipelineConfig` 新增 `budget_area`(默认 0.0) / `budget_floors`(默认 1) / `do_budget_from_bom`(默认 True)。
`run()` 在「[4] 说明」与「[5] 审查」之间插入 `[4.5] 清单报价（图纸驱动）`：
**仅当 `budget_area > 0` 才触发**，否则干净跳过（保持历史默认行为，零回归）。
`PipelineResult` 新增 `budget_from_bom_file` / `budget_from_bom_total` 两个字段（可序列化）。

### §23.4 ★ 实测：同一张图，两种报价的差距
样本：`pipeline("12x8米三层住宅平面图带客厅厨房卧室")`，建筑面积 288 m² / 3 层。

| 路径 | 总造价 | 单方 | 数据来源 |
|---|---|---|---|
| 方案级（面积×含量）`budget.md` | **554,479 元** | 1,925 元/m² | 建筑面积 × 经验含量 |
| **图纸驱动（bom 几何量）`budget_from_bom.md`** | **878,067 元** | 3,049 元/m² | 墙长 168m→砌块 308m³ + 门窗 536m² + 柱/梁/板/基础→混凝土 10.7m³ + 钢筋 1.31t（6 项几何推导）+ 6 项面积回退 |

→ 图纸驱动比方案级贵 **58%**：因为它按"真画的墙长"算砌块，而方案级只按"建筑面积×0.38"
平摊混凝土，**漏掉墙体体积这种大头**。这正是"贴这张图"与"拍脑袋平方数"的本质差别。

### §23.5 ★ 诚实边界（必须说清）
1. **换算规则是经验假设，非 16G101 定额**：墙厚/层高/含钢量/损耗都是 `ConvertParams` 里的
   可配经验值，不是国标清单定额；把它当"估算口径"而非"招标/结算依据"。
2. **图纸没画的，绝不虚构**：纯平面图（只画墙/门/窗/柱）不画梁/板/基础层 → 混凝土/钢筋/模板
   几何量低 → **自动回退面积经验含量**，并在 `cov` 报告里标「回退(面积)」，不假装从图里量出来。
3. **钢筋仍是混凝土体积×含钢量**的近似，非逐根算量。
4. 与方案级报价一致：**仍非招标/结算依据**，只是从"面积拍脑袋"升级到"图纸几何驱动"。

### §23.6 验证
- `examples/validate_pipeline_budget.py`（新增）：用例 A（`budget_area=288`）→ 产出
  `budget_from_bom.md` + `coverage.md`，成功；用例 B（`budget_area=0` 默认）→ 干净跳过，零回归。
- `examples/_demo_4deliver.py`：复跑数字**完全稳定**（55.4 万 / 单方 1925 vs 87.8 万 / 单方 3049，21 项 BOM）。
- 回归：`verify_all.py` 21 套件仍全绿（本次仅新增 `bom_to_budget.py` + pipeline 增量，不碰既有断言）。

**升级结论**：四项交付能力从"能跑"升级到"数据贯通"——
清单报价现在由 `bom.py` 的真实几何量驱动，并附数据溯源报告，明确每笔量来自图纸还是面积回退。

## 二十四、v1.17.11 收口（增强2 接单价库 + 增强3 识图兜底）

来源：桌面 `接单价库.txt`（用户草稿）→ 落地 + 跑通。草稿含接口错配，已按真实代码修正。

### §24.1 增强2：接单价库 `scripts/price_library.py` + `scripts/price_library_nanning.py`

**解决的问题**：`budget.py` 的 `UNIT_PRICES` 是写死的经验常数，用户无法替换成自己的市场价/地区定额。

**关键修正（草稿→落地，避免静默空操作）**：
| 草稿（错） | 落地（对） | 原因 |
|---|---|---|
| `to_budget_prices()` 返回 `{code: {'name','unit','price'}}` | 返回 `{name: (unit, price)}` 元组 | `budget.py` 的 `UNIT_PRICES` 是**按材料名**索引、**(单位,单价) 元组**值；code 做 key 会整体 miss → 静默空操作 |
| `create_default_library()` 硬编码 28 项 | 实时从 `budget.UNIT_PRICES` 迁移（12 项，name 对齐） | 与 `budget.py` 单一事实源一致，避免两套常数漂移 |
| 自测导出用 `/tmp/` | 用 `tempfile.gettempdir()` | Windows 无 `/tmp` |

**覆盖入口（两条等价路径）**：
- (A) 显式覆盖：`ov = lib.to_budget_prices()`，传给 `QuantityCalculator(..., unit_prices=ov)` 或 `estimate_from_bom(..., unit_prices=ov)`；
- (B) 全局替换：`lib.apply_to_budget(budget_module)` 直接 `UNIT_PRICES.update(...)`。

**南宁库**：`price_library_nanning.py` 的 `create_nanning_library()` 覆盖 `钢筋 HRB400(t,3400)` / `商品混凝土 C30(m³,420)` / `水泥 42.5(t,317)` / `中砂(m³,105)` 4 个主材（其余走默认），外加 13 人工 + 3 机械（人工/机械不参与本版预算，仅作库完整性）。

**★ 实测（覆盖确实生效，非静默空操作）**：`examples/demo_price_lib.py` 四路对比（12×8 三层 / 288 m² / 3 层）

| 路径 | 总造价 | 单方 | 变化 |
|---|---|---|---|
| A 方案·默认 | 554,479 元 | 1,925 元/m² | — |
| B 方案·南宁 | 531,046 元 | 1,844 元/m² | **−4.2%** |
| C 图纸·默认 | 878,067 元 | 3,049 元/m² | — |
| D 图纸·南宁 | 874,337 元 | 3,036 元/m² | **−0.4%** |

→ 南宁单价代入后总价变化，证明不是静默空操作；差值偏小因仅覆盖 4 主材，符合诚实预期。

**诚实边界**：南宁材料价为 2026-09 市场参考价（我的钢铁网），非广西 2026 版官方定额；人工为南宁造价信息 2026-01 区间中值。正式造价须用官方《预算清单综合单价组价表》。

### §24.2 增强3：识图兜底 `scripts/reader_fallback.py`

**解决的问题**：external 真实图图别判错 → 施工说明套错专业模板（如把火灾报警图套成平面图话术）。

**草稿接口错配 → 修正**：
| 草稿（错） | 真实代码 | 落地修正 |
|---|---|---|
| `DrawingUnderstanding` 类 | 不存在；真实是 `drawing_reader.DrawingInfo` | 读取 `DrawingInfo` 真实字段 |
| `.confidence` / `.margin` | 真实是 `.type_confidence` / `.type_margin` / `.abstained` | `_extract()` 按真实字段名抽取，兼容旧名 |
| `patch_pipeline_fallback` 改 `Pipeline._step_notes` / `self.result._understanding_obj` | 这俩方法/属性不存在 → monkeypatch 必崩 | 改为 `_step_notes` 内直接调用 `ReaderFallback.evaluate_from_info(self.result.understanding)` |

**判定逻辑（沿用 v1.17.9 实证：margin 才有判别力）**：
- `margin ≥ 0.2 且 confidence ≥ 0.7` → 高（直接用）
- `margin ≥ 0.1` → 中（建议人工确认）
- 否则 / 弃权(`abstained` 或图别="未识别") → 低 / 未知（必须人工确认）

**主链接入点**：`pipeline._step_reader` 落盘 `understanding` 时新增 `type_margin` / `abstained` 两字段；`_step_notes` 在 `source=="识图匹配"` 且 `needs_review` 时，把 `notes_key` 回退为 `floor_plan`、`source` 改 `识图兜底`、写 `notes_warning`，并在 `construction_notes.md` 顶部以 `> ⚠ ...` 警示。`PipelineResult` 新增 `notes_warning` 字段。`reader_strict` 配置可切换严格阈值（默认关闭）。

**★ 实测**：`examples/demo_reader_fallback.py`
- 单元：`DrawingInfo`/dict 两种数据源，高/中/低/未知全部命中预期；
- 集成：真实调用 `pipeline._step_notes`——场景A（火灾报警图 conf=0.5/margin=0.0）回退 `floor_plan` + `识图兜底` + 警告入文件；场景B（高置信）保留原模板、无警告。

### §24.3 验证汇总
- `scripts/price_library.py` / `price_library_nanning.py` 自测：12 / 20 项，格式对齐 ✅
- `scripts/reader_fallback.py` 自测：5 用例全部断言通过 ✅
- `examples/demo_price_lib.py`：A/B/C/D 四路对比，覆盖生效 ✅
- `examples/demo_reader_fallback.py`：单元 + 集成全部通过 ✅
- 回归 `examples/validate_pipeline_budget.py`：用例A 219,319 元（图纸驱动）、用例B 干净跳过，零回归 ✅

**升级结论**：报价从"写死经验常数"升级到"可配市场价/地区定额"（增强2）；施工说明从"盲信识图图别"升级到"不确定即回退通用模板 + 写警告"（增强3）。两增强均 opt-in、零回归。

